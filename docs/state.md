# Ditto — Current State

**Last updated:** 2026-05-19 (Builder — Brief 293 (Background Watch Engine Skeleton) implemented + fresh-context reviewed APPROVE-WITH-FOLLOWUPS → all 4 FLAGS now fixed (FLAGs 1/2/3 inline this session, FLAG-4 parent 275 D14 line-number drift corrected on "fix all" follow-up); 60/60 targeted vitest + root+core type-check green.)

> **Compaction note (2026-05-16):** This file had grown to 4156 lines / ~1MB — ~52× the CLAUDE.md "~80 compact lines" mandate — because session-by-session narratives and per-session retrospectives accreted here instead of distilling into the durable snapshot. This is now a true snapshot. **Full pre-2026-05-16 session log + all historical Documenter retrospectives are preserved in git** — recover via `git log -p docs/state.md` (the last 4152-line revision is the pre-compaction snapshot; it is the working-tree HEAD until this compaction commits). Phase-level history lives in `docs/changelog.md`; the capability map lives in `docs/roadmap.md`; durable system design lives in `docs/architecture.md`. Keep this file compact — new sessions add a one-line rolling entry below and update the live sections; they do **not** append narrative blocks.

## Rolling Update Log (most recent first — one line each)

- **2026-05-21** — **Architect — ADR-050 (Surface Primitives vs. Engine Primitives — Workspace User-Facing Inversion) DRAFTED & fresh-context Reviewer → APPROVE-WITH-REVISIONS; all 2 blockers + 3 should-fix items resolved inline; pending human approval.** Ratifies the surface/engine vocabulary split the user pinned during the 2026-05-21 unified-chat refactor conversation: workspace UI speaks Surface (Chat, Alex/Mira, Pathway, **Agent**, Work Item, Review, Outcome, Confidence) per industry vocabulary; engine speaks Engine (Process, **Executor**, Skill, Trust Tier, Memory Scope, Work Item, Harness). User instruction: "we need language that is commonplace — agents is the language of the day around AI that does things for the user." Surface "Agent" = ADR-037 **Hired Agent** (not bare Process) — Hired Agent runs one or more Processes; the `agents` table is correctly named. Engine rename targets the Layer-2 step-executor only (`ai_agent`/`cli_agent` literals, `StepDefinition.agent_role`, `"claude-managed-agent"` runner kind, "Agent Layer" docs, "Agent Card" runtime-inspector card). `StepAdapter`/`SystemAgentHandler`/`MemoryProvider`/`EngineConfig` interface names unchanged; `agents`/`AgentStatus`/`AgentCategory` exports unchanged (they're surface Hired Agents). ServiceOS impact corrected from "none" to "yes — `StepExecutor` literals + `agent_role` field + runner-kind are consumer-visible; rename brief must include coordination note." Self stays `Self` in engine; Alex/Mira are per-workspace persona *configurations* of Self (decided in ADR, not punted). Drill-down/demote-not-delete rule established. ADR-024 nav (Today/Inbox/Work/Projects/Routines) reconciled: superseded by Brief 280 IA inversion + this ADR for any engine-vocabulary destinations. Provenance corrected to acknowledge DDD bounded-context ubiquitous-language pattern (adopted); the specific mapping and Hired-Agent carve-out are Original. Reviewer was a fresh-context Sonnet agent (maker-checker honoured) — surfaced ADR-037 contradiction + enumerated 6 missed `@ditto/core` rename surface points + nav reconciliation gap + Self open-question-should-be-decided, all real, all fixed. Roadmap-referenced but not-on-disk: ADR-046, Brief 247, Insight-222 (called out in §Related). Next: human approve → 3 follow-ups (architecture.md "Surface vs Engine" section; dictionary entries split; rename brief for `ai_agent`→`ai_executor` with ServiceOS coordination); after that, Brief 268 + Brief 247 reshape becomes mechanical. No engine/code changes this session (Architect produces documents only).
- **2026-05-21** — **Architect — ADR-045 (`IntegrationProvider` as the Engine ↔ Broker Boundary in `@ditto/core`) DRAFTED & fresh-context Reviewer → APPROVE-WITH-REVISIONS; all 3 blockers fixed inline (Insight-215 two-regime stepRunId contract, `resolveConnection` restored as third method, interface+wiring ship together not deferred); awaiting human approval.** Responds to ServiceOS (downstream `@ditto/core` consumer) 3-ask proposal — accepts `IntegrationProvider` interface narrowly, redirects `workspace_integrations` table to consumer schema, redirects `@ditto/integrations-*` packages to consumer's npm scope. Reaffirms ADR-031 (Composio DEFERRED for Ditto, Network Service stays the broker). Interface is 3 methods (`listTools`, `resolveConnection`, `executeTool`), provider-agnostic, no Composio/AgentMail shapes in core. `scopeId` opaque string. `EngineConfig.integrationProvider?: IntegrationProvider` — singular not array (canonical, not deferred). Tool-name convention asserted: `domain.verb_noun`, never broker-namespaced. `stepRunId` required string, Insight-215 two-regime contract: real `stepRuns.id` for external side-effecting tools, sentinel acceptable for read-only / pre-flight. Step-execution wiring ships in same brief as interface (no silent no-op). Counts as one signal toward ADR-031 Nango re-evaluation trigger; Dev PM logs in weekly triage. Next: human approve → implementation brief (8-12 ACs, one builder session) once ServiceOS confirms shape OR Ditto product layer adopts the seam. No code changes this session (Architect produces documents only).
- **2026-05-19** — **Builder — Brief 293 (Background Watch Engine Skeleton, first sub-brief of parent 275) IMPLEMENTED & fresh-context Dev Reviewer → APPROVE-WITH-FOLLOWUPS; FLAGs 1/2/3 fixed inline this turn, FLAG-4 doc drift left for Architect (Insight-043); pending human disposition.** Engine-only sub-brief in the 3-way 293/294/295 split per Architect parent 275 rulings D1–D17. Delivered: 4 Network-tier tables (`network_background_watches` + `consecutiveQuietRuns` for 294, `network_watch_runs`, `network_watch_proposals` as **thin join to `networkPossibleConnections`** per D4/FLAG-7, `network_watch_dismissals` w/ three-state `kind` per D14); migration `0017_sturdy_the_captain.sql` claimed idx 17 (Insight-190 re-verified); pure 8-rule `network-health.ts` evaluator (blocked / anti-persona / over-contact / requester-over-asking / duplicate-cooldown / stale-evidence / commercial-sensitive / low-confidence) with `suppress > queue-for-review > downgrade > pass` precedence; sense→assess→act→gate→land→learn→brief `runBackgroundWatch` runner reusing Brief 274 `runNetworkSearch` (injectable matchFn/scoutFn per D2, no duplicate ranking) + 4h manual-only "run now" cooldown per D17 (`MANUAL_RUN_COOLDOWN_MS`) + null `ianaTimezone` → UTC fallback per D11; hourly Network-deployment `selectDueWatches` sweep wired in `src/engine/scheduler.ts` (passes captured `now` to both selectDueWatches + runBackgroundWatch per FLAG-2 fix); `requireNetworkStepRunId({rejectWebDirect: true})` guard on `/api/v1/network/watches/*` (POST `run|pause|resume|close|create`; PATCH for renames) — rejects caller `stepRunId` by key-presence including falsy null/""/0/false (Insight-232) AND validates action enum BEFORE minting step run (Insight-239); tool-resolver registers `run_network_background_watch` + filters all contact tools (`gmail.send_message`, `send_claim_invite`, `send_recipient_approval_email`, `send_requester_approval_email`, `send_follow_up_email`, `create_intro_thread`, `fan_out_intro_feedback`) when `isBackgroundWatchStepRun(stepRunId)` returns true — capability-by-transport per Insight-235 (routing-invariant, not table-membership); legible cycle YAML at `processes/cycles/network/network-background-watch.yaml` deliberately under a subdirectory so workspace-tier `loadAllProcesses` top-level scan does NOT pick it up (resolves cross-tier deployment problem D1); `network-watch` ≤12 starts/user/hour rate limit reused from `network-abuse-controls.ts`. Reviewer FLAG-1 fixed: `selectDueWatches` SQL `where` now includes `isNotNull(nextRunAt)` AND `lte(nextRunAt, now)` so future-due and `manual_only` (null nextRunAt) watches are correctly excluded (AC #12). FLAG-3 fixed: two new tests in `src/engine/network-background-watch.test.ts` ("excludes watches whose nextRunAt is in the future (AC #12)" + "excludes watches whose nextRunAt is null (manual_only cadence)") + existing UTC-fallback / paused-closed tests updated to seed past `nextRunAt: dueAt` so they survive the new filter. FLAG-4 (parent D14 cited stale line numbers L426/L443 for the pre-existing `watch_lifecycle_changed` / `watch_feedback` audit classes — actual lines are L551/L568) fixed on "fix all" follow-up: D14 in `docs/briefs/275-background-watch-network-health.md:138` rewritten to cite the current line + explicitly state both values pre-date Brief 293 and are reused, not added. Routing-invariant smoke-test specifically asserted in `network-background-watch.test.ts`: `resolveTools([7 forbidden contact tools], ..., watchStepRunId)` returns `tools: []`; non-watch step runs are unaffected. `process-loader.test.ts` gained "does NOT load the Background Watch YAML" so a future accidental promotion to workspace-process is caught. Gates: root `npx tsc --noEmit` 0, core `npx tsc --noEmit` 0, targeted `pnpm vitest run` on the 4 Brief 293 files **60/60**. Full-suite vitest had 9 pre-existing flaky failures (network-match / workspace-provisioner / network-discovery-runtime / network-chat-workflow / bridge-server.spike port 3457 / work-items status route timeouts under resource contention) — all reproduced as resource-contention flakes and pass when run in isolation with `--testTimeout 30000`; **none caused by Brief 293**. Maker-checker honoured: fresh-context Reviewer (Sonnet, isolated, independently reproduced every gate) returned APPROVE-WITH-FOLLOWUPS (4 flags, 3 fixed inline, 1 left for Architect). Next step: human disposition → if approved, `/dev-documenter` for retrospective + Architect picks up FLAG-4 doc drift, or `/dev-builder` Sub-brief **294** (digest delivery — depends on this brief's `network_watch_runs` table + `consecutiveQuietRuns` column).
- **2026-05-19** — **Documenter — Brief 291 COMPLETE.** Moved the brief to `docs/briefs/complete/291-attribution-visitor-conversion.md`, updated roadmap status/link, recorded the host-bound signed-cookie pattern in `docs/landscape.md`, and added the closeout retrospective below. Preserved the dirty-worktree cleanup outcome: `0015_parched_ultimates.sql` is Brief 289-only, `0016_share_attribution.sql` is Brief 291-only, and 293/294 migration references now start after idx 16. Also aligned `network-audit.test.ts` with the active Brief 293 background-watch audit enum so the shared 122-test regression set reflects the current dirty tree. Verification: `pnpm exec drizzle-kit check --config=drizzle.network.config.ts`, focused Brief 291 vitest, `pnpm run type-check`, and `git diff --check` green.
- **2026-05-19** — **Builder/Reviewer — Brief 291 review-fix pass complete after fresh-context review FAIL.** Fixed all functional blockers: `POST /api/v1/network/share-attribution` now requires and verifies the signed `ditto_share_ref` landing cookie before rate-limit/mint/sign/write and rejects missing/tampered/mismatched cookies; workspace-mode middleware now allows `/welcome` and `/network/request` only for valid host-bound `?ditto_ref=` handoffs; destination pages verify the token against the `{handle}.ditto.you` host; the CTA strip no longer disables the four CTAs and now carries explicit `SuggestionBlock` / `ActionDef.payload` intent/referral metadata. Cleaned migration sequencing so `0015_parched_ultimates.sql` is Brief 289 intro feedback/outcome only, `0016_share_attribution.sql` is Brief 291 only, and background-watch moves to `0017_sturdy_the_captain.sql`. Verification: focused Brief 291 vitest **122/122** and `pnpm run type-check` **0**. Remaining documented limitation: Playwright visitor-conversion e2e still not run because local `/people` server render requires a live Network DB.
- **2026-05-19** — **Builder/Reviewer — Brief 289 (Intro Reply Ingestion, Follow-Up, and Outcome Capture) fresh-context review completed: REVISE → blockers fixed → APPROVE-WITH-NITS; final nit fixed.** Review found and the Builder fixed: live inbound intro reply path was not wired, prepared compliance headers/workspace sender were dropped before send, non-member follow-up recipients were unsupported, sender authentication for thread matches was incomplete, and the generated migration carried the dirty-tree `network_share_attribution` table. The behavioral blockers are fixed: `processInboundEmail` now attempts authenticated intro-thread matching and mints `network-intro-inbound-reply` before `recordIntroFeedback`; follow-up sends preserve prepared headers and support direct non-member recipient email; spoofed senders are rejected; `recordIntroFeedback`/fan-out reject non-server-minted lane step IDs before writes. Reviewer re-review returned **APPROVE-WITH-NITS**; the remaining nit is now covered by a direct live-seam regression in `src/engine/inbound-email.test.ts`. Verification: focused Brief 289 vitest **86/86** and `pnpm run type-check` **0**.
- **2026-05-19** — **Builder — Brief 291 (Attribution + Visitor Conversion) IMPLEMENTED; later fresh-context review fixes are recorded above.** Added `packages/web/lib/signed-cookie.ts` (Edge-compatible HMAC, no dependency) and middleware landing capture for `/people/{handle}?ref={channel}` → signed `ditto_share_ref` cookie carrying `{channel, ph, ts}` only. Added pure `inferVisitorIntent` + keyword extraction in `src/engine/visitor-intent-inference.ts`, wired in-process into the public profile chat route (no self-HTTP, no wrapper run). Added additive `VisitorCtaStrip` in the existing profile aside/desktop + below-chat/mobile path. Added `networkShareAttribution` table + `share_attribution_recorded` audit class + `POST /api/v1/network/share-attribution`; migration now lives separately at idx **16** (`0016_share_attribution.sql` + snapshot) after the cleanup pass above. Updated `docs/landscape.md` to record the shipped in-house signed-cookie helper. Initial verification: `pnpm run type-check` 0; targeted vitest **117/117**. Playwright visitor-conversion e2e not run/added because the server-rendered `/people` page still requires a live Network DB in the test server.
- **2026-05-19** — **Builder — Brief 289 (Intro Reply Ingestion, Follow-Up, and Outcome Capture) IMPLEMENTED; fresh-context review pending.** Added `IntroFeedbackParty` / `IntroFeedbackEventType` / `IntroOutcomeClass` / 11-value intro feedback taxonomy in `@ditto/core`, extended `IntroductionState` with `feedback-collected`, added `network_intro_feedback`, `network_outcome_metrics`, feedback timestamps on `introductions`, and migration idx **0015**. Extended `inbound-email.ts` with intro-context keyword classification + outcome-class seeding and thread/reference matching against warm intro and approval-message metadata. Added `intro-feedback.ts` append-only recording with audit row (`intro_feedback_recorded`), terminal state update, one-time +30d retry for `outcome:no-outcome-yet`, additive Member Signal claim fan-out, anti-persona refinement, and outcome metric upsert. Added follow-up template + `sendFollowUpEmail` + scheduled-step runner (no new scheduler primitive), compliance gate, workspace sender continuity, and per-party sends. Added `/api/v1/network/intros/[id]/feedback` with key-presence `stepRunId` rejection, validate-before-mint for event/category/outcome, token verification, 5/email/hour rate limit, ambiguous-to-chat no-write behavior, and server-minted wrapper run. Chat surface now shows outcome capture and prior feedback after `thread-sent`. Tool resolver registers `send_follow_up_email`, `record_intro_feedback`, and `fan_out_intro_feedback`. Checks: `pnpm vitest run src/engine/inbound-email.test.ts src/engine/intro-feedback.test.ts src/engine/intro-followup-email.test.ts src/engine/intro-followup-scheduler.test.ts 'packages/web/app/api/v1/network/intros/[id]/feedback/route.test.ts'` → **83/83**, `pnpm run type-check` → **0**. Review-agent step not run in this Builder turn because current execution policy requires explicit user permission for sub-agents; next step is `/dev-reviewer` or explicit fresh-context review request.
- **2026-05-19** — **Architect — Brief 275 (Background Watch & Network Health) DESIGN COMPLETE & fresh-context reviewed PASS-WITH-FLAGS → all 7 FLAGS + actionable nits resolved inline; human APPROVED 2026-05-19 — all 5 ratification items confirmed per Architect recommendation, handed to `/dev-builder`.** Synthesized the Researcher report (`docs/research/275-…md`, 20 §) + Designer UX spec (`docs/research/275-…-ux.md`, 28 §) + human Q1 disposition (cycle YAML lives in Network repo) into rulings **D1–D17 + OQ-1/2/3**. Parent **Brief 275** rewritten as the design-coherence reference (carries D1–D17, OQ-1/2/3, the 7-job UX model, surfaces A–H+B′, and the original 18-AC rollup *redistributed* to sub-briefs — parent carries no ACs of its own). Split per Insight-004 (original >17 ACs / >3 subsystems) into **Sub-brief 293** (engine skeleton — runner + `network-health.ts` 8-rule evaluator + 4 tables + hourly Network sweep + guarded tool-resolver + watches route + legible cycle YAML; **17 ACs**, at the ceiling — parent Cross-Cutting Risk documents why not re-cut), **Sub-brief 294** (cross-tier digest delivery via Brief 288 `network_workspace_deliveries` `watch-digest` kind + composer + token-auth unsubscribe route — **12 ACs**), **Sub-brief 295** (Review + Curate UI + three-state dismiss + feedback writer + operator surface — **~16 ACs**). Build order **293 → 294 → 295 → close parent 275**; linear dependency, mirrors 276→288/289 and 277→290/291/292 precedents. Load-bearing decisions: **D1** per-watch `nextRunAt` + hourly Network-tier sweep; cycle YAML is a legible artifact **NOT loaded by workspace `loadAllProcesses`** (resolves the cross-tier problem); **D2** reuse Brief 274 `runNetworkSearch` (injectable `matchFn`/`scoutFn`); **D4** `network_watch_proposals` is a **thin join to `networkPossibleConnections`** (not a duplicate — FLAG-7 fix; Brief 274 card renders from PC lifecycle `watched`); **D7** digest crosses tier ONLY via Brief 288's durable substrate (Insight-234, no parallel path); **D8** empty-state suppression + `renderToStaticMarkup` + cap ≤3; **D11** `ianaTimezone` populated at watch-create from client `Intl.DateTimeFormat`, null→UTC fallback; **D12** no-contact enforced by tool-list transport boundary (Insight-235, routing-invariant test not table inspection); **D13** reuse Brief 289 `IntroOutcomeClass` as the single shared outcome enum + separate optional `engagementSignal` (FLAG-3 — departs Designer D-Q7, **ratified 2026-05-19**); **D15** caller-supplied `stepRunId` rejected via `hasCallerStepRun` key-presence (`search/route.ts:64`); **D16** privacy scrubber on every surfaced field, admin renders coded/boolean only; **D17** manual run-now 4h cooldown + v1 explicit-feedback-only + abuse limit = watch-run starts/user/hour. Fresh-context Dev Reviewer (Sonnet, isolated) returned **PASS-WITH-FLAGS** (no hard FAILs), 7 FLAGS — every citation independently verified via Bash/Read before acting (citation-drift defect class avoided), all accurate, **all resolved inline**: FLAG-1 (rephrase to the real `hasCallerStepRun` key-presence mechanism, no separate truthiness check), FLAG-2 (`ianaTimezone` data-flow specified — capture at create, null→UTC, tested), FLAG-3 (NO competing `networkWatchOutcomeClass` — reuse Brief 289 `IntroOutcomeClass` shared enum + `engagementSignal`; whichever of 289/295 lands first defines, other imports), FLAG-4 (293 density: coupled by construction, schema-only sub-brief would have no behavioral AC — documented in parent, not re-cut), FLAG-5 (admin failure surface must be coded/boolean only — `NetworkHealthDecision` per-rule reason text + non-owner identity excluded; Brief 261 Hard Rule #5, operator = non-owner; new 295 AC-8a), FLAG-6 (RFC 8058 one-click unsubscribe arrives with no session → signed `magic-link`-pattern token + dedicated `/api/v1/network/watches/unsubscribe` token-auth route, NOT the session-auth watches route), FLAG-7 (thin-join fix above). Also resolved reviewer-flagged opens: OQ-7 (abuse limit = run-starts/user/hour), D-Q2 (4h manual cooldown), D-Q4 (v1 explicit-feedback-only), nit-1 (293 smoke specifies no-contact assertion: zero `network_workspace_deliveries`, `notifyUser` spy zero, zero outbound-audit), nit-4 (`not-now` disambiguated: `introductionStateValues` L231 Brief 288 vs `networkSearchFeedbackKindValues` — unrelated enums), nit-5 (`consecutiveQuietRuns` column added in 293, read by 294). **Items RATIFIED by human 2026-05-19 (all per Architect recommendation):** FLAG-3/D13 (two-field taxonomy supersedes Designer D-Q7 for v1); OQ-1 (post-intro outcome capture optional); OQ-2 (no cross-watch learning v1); OQ-3 (Curate = Privacy Center sub-section, **OVERRIDES** Designer top-level-nav, closed for v1); FLAG-4 acknowledged (293 intentionally the dense sub-brief). Parent 275 + 295 OQ/D13 framing converted open→ratified inline (Insight-043 doc accuracy). **Migration idx shifted:** Brief 291 owns `0016_share_attribution`; 293 background watch now owns idx 17 / 294→idx 18 / 295→idx 19 (Insight-190 re-verify-at-build retained). Maker-checker honoured (Reviewer = separate fresh-context agent; independently verified the 18-AC rollup has no drop/double-own/orphan + checklist coverage). Docs-only; no engine/code changes (Architect produces documents only). Next step: `/dev-builder` **Sub-brief 293 first** (engine skeleton; 294/295 depend on its tables + runner; claim journal idx 17, re-verify per Insight-190).
- **2026-05-19** — **Documenter — Brief 290 (Share Studio + Channel Variants) CLOSED OUT: `/dev-review` 5-pass post-review folded, brief moved to `complete/`, Insight-204 recurrence captured, retrospective done.** After Builder APPROVE, a report-only `/dev-review` (5 passes over all 290 changed files) surfaced **6 non-blocking items the maker-checker reviewer missed**, all fixed in-scope under `fix all`: (3.1) `share-modal.tsx` close-reset effect now clears *all* derived state (`requestedRef.clear()`, `setCache({})`, `setStatusByChannel(idleStatuses())`, `setEditedByChannel({})`) via an `idleStatuses()` factory + `STUDIO_PANEL_ID` const; (5.1) full WAI-ARIA tabs pattern added — `onTabKeyDown` (Arrow/Home/End roving), `role=tablist/tab/tabpanel`, `aria-controls`/`aria-labelledby`, roving `tabIndex`, `tabRefs` focus mgmt (does not break the e2e `getByRole` queries); (5.2) no-autopost doctrine comment over the "Post to X" button; (4.1) `story-card-png/route.ts` gains `Content-Disposition` attachment filename + `public, max-age=3600, stale-while-revalidate=86400` cache header; (5.3) `share/route.ts` clarifying comment that `"website-badge"` in the allow-list is intentional (engine short-circuits, auditing is not a leak) — no behavior change. Post-fix gate re-green: root+core type-check 0, targeted vitest **41/41**, Playwright `network-share-studio` **1/1** from repo root. **The fetch-effect cancellation race fixed during the Builder session was recognized as a recurrence of Insight-204** (loading/status flag in effect deps re-fires and stomps in-flight state) — appended a "## Recurrence — Brief 290 (2026-05-19)" section to `docs/insights/204-…md` rather than creating a duplicate insight; this is the **2nd independent occurrence**, invisible to both static review and the fresh-context maker-checker, caught only by the interaction e2e — confirms the insight's proposed lint-rule "future consideration" is worth building. Brief **290 `mv`'d to `docs/briefs/complete/`** (Status header → COMPLETE; parent 277 + sub-briefs 291/292 correctly remain in `docs/briefs/`). Reference-doc gap audit (Insight-043): added **Share Studio / Share Variant / Website Badge** entries to `docs/dictionary.md` Network Superconnector Doctrine glossary (new canonical user-facing nouns shipped this session, previously absent). **Flagged for the Architect (NOT applied — Documenter doesn't change architecture):** Insight-204's own "Where It Should Land" items remain UN-applied — the architecture.md Layer 6 "stream completion must not stomp state" rule and the review-checklist Layer-6 line; Insight-204 stays **active** until absorbed. Roadmap row 277 already Builder-updated; left as-is (290 done, parent still in flight via 291/292). Next step: `/dev-pm` for next work, or wrap.
- **2026-05-19** — **Builder — Brief 288 (Intro Consent State Machine + Decision Emails) IMPLEMENTED & remediated; prior Dev Reviewer REJECT (B1–B4) all closed → fresh-context Dev Reviewer re-review → APPROVE-WITH-NITS; pending human disposition.** Two-sided consent state machine (`proposed→requester-approved→recipient-asked→recipient-approved→thread-sent` + `declined`/`not-now`) with `IntroProposalCardBlock` engine-first in `@ditto/core` and `introductions` extended additively (Brief 276 D1/D2 schema). **Four prior REJECT blockers closed, each re-verified non-vacuous by an isolated fresh-context reviewer:** **B1 (AC#17)** `intro-proposal-card` added to workspace-inbox-import `IMPORTABLE_KINDS`, idempotent by `(action,entityType,entityId)`, unrecognized kinds left un-ACKed so a future deploy can still pick them up (Insight-234). **B2 (AC#18)** cross-deployment terminal-state PATCH back to `/api/v1/network/intros` via `handleOutboundConsentPatch` — mints a wrapper step-run per POST, rejects caller-supplied/falsy/UUID-shaped `stepRunId` (Insight-232) and validates the consent-action allowlist *before* minting (Insight-239), Insight-180 step-run guard on the side-effecting recorder. **B3 (AC#19)** new `src/engine/intro-flow.integration.test.ts` drives real `proposeIntroduction → recordRequesterApproval → recordRecipientApproval` against live PGlite (no DB mock, no injected compliance on the requester→recipient hop — real `classifyAndPrepare` runs on a clean store) asserting exactly 4 audit rows / exactly 2 first-writer-wins deliveries / terminal columns, plus the requester-decline-is-terminal matrix (recipient never asked). **B4 (AC#11)** `scrubProposalCardForRecipient` nulls `antiPersonaMd` on every embedded `network-profile-card` on every non-owner render path, edit-draft private-claim injection refused before any state write; unit suite in `intro-proposal.test.ts`. Audit-step-run coupling solved in B3 via rootDir-less `createNetworkLaneStepRun` + `NETWORK_KB_ROOT`=temp dir so JSONL append and audit lookup share one directory. **Three non-blocking reviewer notes closed as in-scope completeness (boil-the-ocean, behaviors the ACs already require — not scope creep):** #1 chat refinement surface (`packages/web/app/network/intros/[id]/chat/page.tsx`) now applies `scrubProposalCardForRecipient` when `party==='recipient'` before render (forward-safety; requester still sees own card); #2 integration test extended with AC#16 delivery-linkage assertions (each delivery `userId` = correct party, `sourceStepRunId` = the minted run, `introductions.requester/recipientDeliveryId` backlink survives the first-writer-wins re-queue); #3 `workspace/inbox/import/route.test.ts` gains an explicit `intro-proposal-card` import + idempotent re-ACK case (imported:1 then imported:0 with no duplicate activities row, re-ACKed both times per Insight-234). **Flagged for the Architect (Insight-043 — Builder flags, Architect fixes; NOT blockers):** #4 Brief 288 says "8-value enum" but the implementation has 7 new outbound `IntroductionState` values; #5 the brief's "What Changes" table omits the consumer-side import + workspace-approval surface that AC#17/#18 actually require. Gates: root `npx tsc --noEmit` 0, core `npx tsc --noEmit` 0, `pnpm run type-check` 0, Brief 288 targeted vitest **30/30** (5 files incl. `intro-flow.integration.test.ts` + both edited test files); full `pnpm test` 3413 passed — the only failures are out-of-scope and non-regression: 2 `memory-assembly.test.ts` p95 perf-tripwire load-flakes (Brief 227; **16/16 in isolation**) + the known Brief 212 `bridge-server.spike.test.ts` spike (MUST NOT fix per scope). Maker-checker honoured: Builder ≠ Reviewer, reviewer ran in isolated fresh context and independently reproduced every gate. Next step: human disposition → if approved, `/dev-documenter` for full retro (and the Architect picks up doc-drift #4/#5), or `/dev-pm` for next work.
- **2026-05-19** — **Builder — Brief 290 (Share Studio + Channel Variants, first sub-brief of parent 277) IMPLEMENTED, fresh-context Dev Reviewer → APPROVE; pending human disposition.** Engine-first: `networkAuditEventClassValues` closed `as const` extended with `share_studio_variant_generated` in `packages/core/src/db/network/schema.ts` (text-array column, no Drizzle migration) + pinned ordered-list test; `network-abuse-controls.ts` registers all three Brief 277 rate-limit names (`share-studio-variant`/`share-attribution`/`outcome-share-consent`) in the closed enum + `DEFAULT_POLICIES` so **291/292 extend, never re-edit** (collision-safe). `generate-share-variants.ts` gains a `channel` param with per-channel branching: linkedin quiet/loud/ask (default linkedin for Brief 260 back-compat), x loud+quiet ≤280 incl. URL, instagram/email-signature single-line ≤200, website-badge short-circuits to fixed "Available through Ditto" text with **no LLM call**; budget-language scrub applied on every channel; only public KB facts enter the prompt. Share route mints a **wrapper step-run per POST** (Insight-232), validates caller input + rejects caller-supplied/falsy/UUID-shaped `stepRunId` bypass **before** minting (Insight-239), audits `share_studio_variant_generated` (subject `network_profile_share`, channel in metadata), rate-limits before mint; engine returns data only (no self-HTTP, Insight-211). New content-free routes: `badge.png` (200×40 pill, no DB load, no rate-limit, immutable cache — AC 9 byte-identical) and `story-card-png` (1080×1920 via `card-silhouette.tsx` `storyMode`, Flexbox-only no `aspect-ratio` per Satori #264). New components: `website-badge-snippet.tsx` (`buildWebsiteBadgeSnippet` pure `<a>+<img>`, URL-encoded handle, `?ref=badge`, no script/iframe), `email-signature-snippet.tsx` (plain-text only — **HTML signature variant deferred per parent §Open**, satisfies AC 10/12), `instagram-story-asset.tsx` (caption + link-sticker + story-PNG download). `share-modal.tsx` rewritten to route `mode="studio"` → `StudioShareModal` (channel-tab strip, channel×voice matrix per AC 5 normative over Designer spec, per-channel client cache via `requestedRef` Set = one POST/channel per Q7, `window.open` never fetch = no autopost per parent AC 7) while preserving Brief 260 `CompactShareModal` byte-for-byte (test-pinned effect dep array `[card, open, retryAttempt, sessionId, variants]`, retry wiring, `aria-label`). Studio wired at the expert owner-card render (`network-chat-shell.tsx`, post-approval entry). **Bug fixed this session:** StudioShareModal fetch effect's cancellation race (status-driven re-render flipped `cancelled` on the in-flight fetch → channel stuck "loading"); replaced with a ref-deduped `loadChannel` callback whose results always apply keyed by channel, effect deps now `[open, activeChannel, loadChannel]`, ref cleared on close, retry deletes-from-ref + reloads. Checks: root `pnpm run type-check` 0, core `npx tsc --noEmit` 0, targeted vitest **41/41** (generate-share-variants + network-audit + share route + share-modal), Playwright `network-share-studio` **1/1** (run from repo ROOT — root playwright.config owns webServer+baseURL; packages/web-local run fails "invalid URL"). Fresh-context Dev Reviewer (maker-checker, isolated, all checks independently reproduced) → **APPROVE**: 11/11 ACs PASS, 26/26 checklist PASS (0 fail/0 flag), 8/8 hard-scrutiny areas PASS; 4 non-blocking findings — (Minor-1) confirm with product that the network-chat expert owner-card render is the intended post-approval Studio entry; (Minor-2) Instagram/email `?ref=` per-channel attribution deferred to **Brief 291** (not a 290 AC); (Minor-3) e2e at `packages/web/e2e/network-share-studio.spec.ts` not brief's `e2e/network/…` — convention-matching, self-flagged per Insight-043; (Nit-4) email HTML variant deferred per parent §Open. Reference-doc drift: e2e path only (flagged, not fixed — Insight-043). Follow-ups for later briefs: card-png/story-card-png in-memory rate-limit; email HTML signature; 291 closes IG/email ref attribution. Next step: human disposition → if approved, `/dev-builder` Sub-brief **291** (depends on 290's now-registered rate-limit + audit enum — must not re-edit), or `/dev-documenter` for full retro.
- **2026-05-19** — **Architect — Brief 277 (Share Loop & Public Profile Conversion) DESIGN COMPLETE & fresh-context reviewed REVISE → 2 MAJORs identified as reviewer tool false-positives, 1 real MAJOR + 3 minors fixed inline; pending human approve/revise/reject.** Synthesized the Researcher report + Designer UX spec into rulings for **Q1–Q9 + Designer OQ1-8**: Q1 variant-gen = extend `generate-share-variants.ts` with per-channel branch (NOT new tool); Q2 attribution = dedicated `network_share_attribution` table + parallel audit row (decoupled from signed chain for Brief 286 indexable `(profileHandle,channel,ts)` queries); Q3 cookie = new `packages/web/lib/signed-cookie.ts` lifting `middleware.ts:79-95` HMAC, **no new npm dep**, lives in `packages/web/` NOT core; Q4 cross-apex = signed `?ditto_ref={channel}|{ph}|{ts}|{hexsig}` query-param (24h, `SESSION_SECRET||NETWORK_AUTH_SECRET`) — Insight-234 does NOT apply (one-shot URL, not durable cross-deployment artifact); Q5 = add `"outcome-share"` to `NETWORK_PRIVACY_SURFACES` for exhaustiveness BUT route composes outcome-specific other-party/deal/request/value suppression (scrubber's `withheldByReason` has no such reasons); Q6 IG = extend `NetworkCardOgFrame` with `storyMode` 1080×1920, Flexbox-only no `aspect-ratio` (Satori #264); Q7 fan-out = per-channel POST + client cache keyed `{channel}` (Q1↔Q7 coupling honored); Q8 badge = pure `<a>+<img>`; Q9 = three NEW rate-limit names. Parent **Brief 277** rewritten as the design-coherence overview (Q-rulings, side-effect matrix w/ Insight-239 mint-validation column, visitor-conversion path table, 4-shape inference + 5-rule fallback, AC redistribution table — parent carries NO ACs of its own). Split per Insight-004 (>17 ACs / >3 subsystems) into **Sub-brief 290** (member-facing Share Studio + channel variants — 11 ACs; ships first), **Sub-brief 291** (visitor attribution + intent inference + cross-apex handoff — 11 ACs), **Sub-brief 292** (outcome-led share + Useful? capture, privacy-critical — 11 ACs). Build order **290 → 291 → 292**. Fresh-context Dev Reviewer (maker-checker, isolated context) returned **REVISE**: design itself VERIFIED SOUND (Q1-Q9 consistency incl. Q1↔Q7, Insight-004 sizing, Q5 division, Insight-234 exclusion, signed-cookie.ts placement, Insights 180/211/232/239 application, side-effect matrix exhaustiveness, Designer UX coverage, provenance, content-block reuse all PASS) but flagged 3 MAJOR + 3 MINOR making it not-buildable-as-written. **MAJOR-1 & MAJOR-2 determined to be reviewer tool false-positives** — reviewer's Glob did not escape `[handle]`/`[id]` route brackets (treated as char-classes) and wrongly reported `profile-chat-client.tsx` + `card-png/route.ts` as nonexistent; both were Read directly this session (70 / 67 lines) and every cited line anchor is correct — NO brief change, documented with evidence. **MAJOR-3 real & fixed** — new rate-limit names not in the closed `networkRateLimitNameValues` `as const` enum + `DEFAULT_POLICIES` (`network-abuse-controls.ts:21-31,117,132`) would fail type-check + throw `unknown_network_rate_limit`; parent Q9 rewritten (registration constraint + policies `share-studio-variant {60,1h}` / `share-attribution {120,1h}` / `outcome-share-consent {10,1h}`), **Sub-brief 290 ships first and owns registering all three names** (added `network-abuse-controls.ts` Work Product row + AC4 + Input #20), 291/292 declare the dependency and do NOT re-edit the closed enum (avoids merge collision), `invite-send` retained not renamed. **MINOR-1** parent split-table AC counts 11/9/9 → corrected 11/11/11 (+ Depends-on notes). **MINOR-2** insight filenames corrected in 290/291/292 §Inputs (`180-steprun-guard-for-side-effecting-functions.md`, `211-no-self-http-from-engine-context.md`, `190-migration-journal-concurrency.md`, `238-curate-is-the-seventh-human-job.md`). **MINOR-3** audit-substrate noun fixed in 290/291/292 ACs — `createNetworkLaneStepRun` JSONL-appends (not a DB row), so "no wrapper-run row written" → "no wrapper-run audit record appended". Also fixed a leftover 290 Review-Process AC-enumeration ("ACs 1–7,10,11,12" → "this sub-brief's ACs 1–11"). Reviewer verdict: "fix the three MAJORs + minors, then this set is APPROVE-ready without re-litigating the design." Curate (Insight-238) carried as provisional in 290 (Define+Decide fallback) and 292 (Orient+Decide fallback) — zero build change either way; 292 is the 2nd cross-surface evidence point. Docs-only; no engine/code changes. Next step: human disposition → if approved, `/dev-builder` starting **Sub-brief 290 first** (it owns the rate-limit registration 291/292 depend on).
- **2026-05-19** — **Researcher — Brief 277 (Share Loop & Public Profile Conversion) RESEARCH REPORT COMPLETE & fresh-context reviewed FLAG → all 5 fixes folded inline; pending human approval before `/dev-architect`.** Persisted `docs/research/277-share-loop-public-profile-conversion.md` — neutral per-work-product build-FROM inventory for the 11 Brief 277 work products (W-1 `generate_share_variants` extension shapes A/B/C; W-2 surface tuple extension for outcome-share; W-3 Studio modal expansion of Brief 260 compact mode; W-4 share-route fan-out shapes A/B; W-5 attribution cookie shapes A/B/C — in-house HMAC vs `iron-session` vs `jose`; W-6 visitor-conversion CTAs via `SuggestionBlock` + `ActionDef.payload`; W-7 CTA renderer with intent dispatcher; W-8 website badge — three shapes `<a>+<img>` / `<script>` / `<iframe>`; W-9 email-signature copy with XSS contract; W-10 IG-story 1080×1920 via `next/og` — shapes A/B; W-11 outcome-share with consent + scrubber surface), 5 cross-cutting patterns (CC-1 `stepRunId` triangle / CC-2 6 new audit event classes / CC-3 admin metrics surface / CC-4 `network-abuse-controls.checkRateLimit` reuse / CC-5 cross-apex domain topology), 6 external-building-block evaluations (EB-1 `iron-session` / EB-2 `jose` / EB-3 `sanitize-html` / EB-4 `langchain-ai/social-media-agent` per-channel prompt registry / EB-5 `next/og` portrait reliability / EB-6 production-SaaS badge embed survey), 6 Original-to-Ditto gaps (recognition-led share psychology / per-channel × per-voice × scrub-aware composition / visitor-intent inference / outcome-share with consent + other-party scrub / cross-apex signed-query-param referral handoff / signal-version coherence — out of scope), 9 open questions for the Architect (Q1 variant-gen shape; Q2 attribution storage; Q3 cookie library; Q4 cross-apex handoff format; Q5 outcome-share surface tuple; Q6 IG story rendering; Q7 variant-route fan-out — flagged Q1↔Q7 coupling; Q8 badge embed shape; Q9 rate-limit name for attribution), full provenance table (in-repo seams cited file:line; externals cited URL; Originals explicitly marked), 6-step smoke-verification block (existing test re-runs + grep locators + optional `next/og` 1080×1920 spike for Q6 de-risk). Updated `docs/landscape.md` with 4 new evaluations under §"Share Loop, Attribution Cookies & Per-Channel Variant Generation (2026-05-19)" (lines 1119-1162): in-repo HMAC pattern (baseline) + `iron-session` + `jose` for cookies, `sanitize-html` for render-time XSS defense (NOT required today — Brief 282 scrubber + React text rendering covers the current surface), `langchain-ai/social-media-agent` constants-registry pattern for per-channel prompt branching. Fresh-context Dev Reviewer returned **FLAG (approve with minor revisions)**: report's structure, neutrality, Designer-Researcher split, and landscape coverage all PASS; 5 mechanical fixes flagged — (1) `SuggestionBlock` line citation drift `content-blocks.ts:341→394` in research report (lines 101, 319) + UX spec companion (line 362); (2) `ChecklistBlock` line citation drift `content-blocks.ts:614→667` in research report (line 320) + UX spec companion (line 365); (3) stale "Pending" status in Reference Doc Status block — landscape.md is updated, not pending; (4) Q1↔Q7 coupling unflagged — Q1 Shape C (deterministic per-channel formatter) forces Q7 Shape B (single POST returns full matrix); (5) missing explicit Insight-234 verdict — no Brief 277 artifact crosses Network → workspace boundary, referral handoff is query-string emission not durable inbox. All 5 fixes folded inline + optional smoke step added (#6 spike `card-silhouette.tsx` at 1080×1920 via `ImageResponse` before committing W-10). Researcher contract honoured: neutral A/B/C shapes throughout, no recommendation, full provenance with explicit Originals. Next step: human disposition → if approved, `/dev-architect` for synthesis of this report + Designer UX spec into Brief 277 implementation design, ruling Q1-Q9.
- **2026-05-19** — **Documenter — Brief 286 `/dev-reviewer` fix-all RECORDED; fresh-context re-review APPROVE; pending human approval.** Builder closed all six reviewer findings: network admin APIs now hard-404 in workspace mode at middleware + route level; `sendNow` claim-invite sends are blocked by global outbound-discovery pause before approval and again in engine sendability; pause-state UI fails closed as checking/unknown instead of active; action-required total now counts queued/drafted candidates beyond the loaded 12 rows; dry-run/pause/approve/suppress/override/reveal all enforce structured reason enums server-side before mint/write; audit metadata scrub now seals broad anti-persona key variants. Fresh re-review returned **APPROVE with no findings**. Verify: focused admin route/component/read-model/claim-invite/middleware vitest **68/68**, `git diff --check` pass. Root `pnpm run type-check` currently blocked by unrelated parallel Brief 276/288 work in `src/engine/intro-proposal.ts` (`@ditto/core/content-blocks` import + implicit `any`), not by these Brief 286 files.
- **2026-05-19** — **Researcher — Brief 275 (Background Watch & Network Health) TECHNICAL-PRECURSOR RESEARCH COMPLETE & fresh-context reviewed PASS-WITH-FLAGS — all 5 corrections applied; pending human disposition.** Human disposition Q1 ("cycle YAML lives in Network repo") consumed as a Researcher constraint and surfaced as a cross-tier deployment problem the Architect must resolve (workspace `loadAllProcesses` scans monorepo + workspace-tier `notifyUser` import). Persisted `docs/research/275-background-watch-network-health.md` — 20 sections covering all 13 Brief 275 work products with file:line citations: (§1 brief intent + persona-lens; §2 constraint map incl. Drizzle next idx **15**; §3 cycle archetype = sense/assess/act/gate/land/learn/brief; §4 cycle YAML 8-step decomposition with handler binding; §5 cross-tier deployment options a-d; §6 scheduler patterns surveyed — `heartbeat.ts:83-104`, `scheduler.ts` node-cron 5-min tick + hourly stale-escalation sweep at L109-122, `pulse.ts` PULSE_INTERVAL_MS=300000, `chain-executor.ts` durationToCron L57-69; §7 NetworkHealthSignal extension — 4 of 8 rules wired, 4 missing flags, 3 interface-shape options; §8 digest HTML rendering — react-dom/server.renderToStaticMarkup vs MJML vs template literal; §9 ranking math reuse of Brief 274 `runNetworkSearch` injectable `matchFn`/`scoutFn`; §10 three-state dismiss gap — `networkSearchFeedback.kind` enum has "not-a-fit" + "hide", missing "not-now" + "wrong-person", 3 options; §10a additional `networkSearchFeedbackKindValues` precedent at schema.ts:296-306; §11 `networkInvitationCandidates.watchId` precedent already present at L1304 + index L1348; §11a-d invitation-candidate provenance + operator-approval columns at L1334-1336; §12 per-user timezone — `networkUsers` has no IANA tz column today, 3 pattern options including hourly-sweep + runner-filter; §13 throttle precedents — `MAX_EMAILS_PER_USER_PER_DAY=5`, `MIN_MS_BETWEEN_NOTIFICATIONS=1h`, `MAX_OUTREACH_PER_PERSON_PER_DAY=3`, `network-abuse-controls.ts` `"network-watch": 12/hour` pre-configured; §14 audit class precedents at schema.ts:419-458; §15 empty-state suppression at `relationship-pulse.ts:443-446`; §16 server-minted step-run minting + caller-supplied falsy rejection (Insight-232); §17 admin reveal pattern; §18 source index for every citation; §19 dictionary impact — Background Watch L1171 + Network Health L1195 already defined, Watch Proposal not yet present; §20 ~10 acceptance-criteria-mapped open questions for the Architect). Updated `docs/landscape.md` with two new entries before "Pragmatic Path for Dogfood": (1) Saved Search / Alert / Digest Patterns — in-house composition of `runNetworkSearch` + `pulse.ts` + `notifyUser`, classification PATTERN/in-house, no new dep; (2) Per-User-Timezone Scheduling — stdlib-only (`Intl.DateTimeFormat` + future `Temporal`), references `scheduler.ts:109` hourly-sweep precedent, no new dep. Fresh-context Dev Reviewer **PASS-WITH-FLAGS** — 1 FAIL (Drizzle journal idx wrong: report claimed "next idx 14" but journal ends at idx 14, fixed in §2 / §4 / Source Index → next idx 15) + 4 FLAGs (FLAG 1: dictionary §19 stale — Background Watch and Network Health already defined; FLAG 2: schema.ts line numbers drifted ~19-24 lines across §10a/§11/§11a/§11b/§11d/§14/Source Index; FLAG 3: §5(c) "user-facing legibility principle flag" parenthetical evaluative; FLAG 4: landscape "recommended pattern" wording evaluative; minor: `isNetworkOperationPaused` L370 not L371) — **all 5 corrections applied with grep-verified line numbers** including additional citation drift caught beyond Reviewer scope (`networkSearchFeedback` L1091→L1117, `networkSearchRuns` L1029→L1024, `networkPossibleConnections` L840→L1062, `networkUserAntiPersona` L790→L816, `networkUserBlockList` L1647→L1690, discovered `circuitBreakerStateValues` enum doesn't exist — only `circuitBreakerAt` timestamp at L1846). No engine/code changes (docs + landscape only). Researcher contract honoured: neutral options, no design decisions, no recommendations. Verify: report consumable by Architect for synthesis; all open questions are concrete and acceptance-criteria-aligned. Next step: human disposition → if approved, `/dev-architect` for synthesis of Researcher findings + Designer interaction spec into Brief 275 design.
- **2026-05-19** — **Architect — Brief 276 (Email, Chat, Consent & Introduction Facilitation) DESIGN COMPLETE & fresh-context reviewed (PASS-WITH-3-FLAGS → all 3 flags fixed); pending human approve/revise/reject.** Parent **Brief 276** rewritten as the design-coherence reference; split per Insight-004 (>17 ACs across >3 subsystems) into **Brief 288** (consent state machine + `IntroProposalCardBlock` in `@ditto/core` + three decision emails + chat refinement surface + approval HTTP route — the skeleton, 19 ACs after flag fixes) and **Brief 289** (reply-classifier extensions + 14-day follow-up email + `network_intro_feedback` append-only table + outcome capture + feedback fan-out to Member Signal/Active Request + outcome metrics for Brief 270 — the flesh, 11 ACs). 11 architectural decisions (D1–D11): (D1) `IntroProposalCardBlock` added to ContentBlock union (closes Designer Flag 1+2 ContentBlock ambiguity; follows `NetworkProfileCardBlock`/`JobRequestCardBlock` precedent); (D2) `IntroStateLog` composes from `RecordBlock`, no new block type; (D3) follow-up sender = same workspace sender as warm intro (continuity); (D4) warm intro thread classified `1:1-intro` with purpose-specific opt-out instead of generic List-Unsubscribe (D5) <200 words body cap + single primary action per email; (D6) every state transition writes one `network_audit_events` row via Brief 282 substrate; (D7) keyword-first reply classifier + chat-fallback for ambiguous (no LLM in inbound hot path); (D8) recipient-approval sender = `mira@{workspaceHandle}.ditto.partners` for continuity (Brief 283 must accept); (D9) follow-up default = 14d, user-configurable; (D10) reply taxonomy = 6 decline + 3 outcome + 1 ambiguous = 11 categories; (D11) edit field labelled "Notes for Ditto to consider" (not impersonation), send-time scrub blocks private-claim injection. Schema impact: extend `introductions` table (Brief 261) additively with `requesterApprovedAt`/`recipientApprovedAt`/`threadSentAt`/`recipientUserId`/`recipientEmail`/`threadMessageId`/`declineCategory`/`followUpCadenceDays`/`recipientDeliveryId`/`requesterDeliveryId`; extend `IntroductionState` enum with 7 new outbound states; extend `IntroductionOriginContext` with `mira-proposed`; extend `NetworkWorkspaceDeliveryKind` with `intro-proposal-card`; add `network_intro_feedback` append-only table + 3 new enums (`IntroFeedbackParty`/`IntroFeedbackEventType`/`IntroOutcomeClass` — last one coordinated with Brief 270). Fresh-context Dev Reviewer (Sonnet, isolated context) returned **22/26 PASS / 3 FLAG / 0 FAIL** — engine-first compliance, side-effect guard matrix, schema-additive check, sub-brief split sanity (288 lands without 289; 289 strictly extends), and Brief 278 D-Q7 trust gates (items #20–#26) all PASS. **All 3 flags fixed before human handoff:** FLAG 1 (Insight-234 cross-deployment delivery durability for recipient workspace IntroProposalCard) → added 3 new ACs to Brief 288 (#16 sender persistence in `network_workspace_deliveries`, #17 idempotent local-import ACK, #18 terminal-state persistence after approve/reject); FLAG 2 (Insight-239 validate-before-mint missing on approve/feedback routes) → added validate-before-mint clauses to Brief 288 AC 13 and Brief 289 AC 10 (`action`/`eventType`/`classifiedCategory`/`outcomeClass` validated against allowed enum *before* `createNetworkLaneStepRun`); FLAG 3 (scheduler provenance unnamed) → Brief 289 provenance row now explicitly cites `src/engine/heartbeat.ts` + the same scheduled-step path Brief 275 background-watch uses, no parallel scheduler. Designer spec coverage: all UX requirements addressed across 288+289; "preview-what-they'll-see-before-approve" is first-class AC; Curate (Insight-238) realized as recipient's "don't introduce me to this kind of person again" opt-out + additive-only feedback fan-out (never rewrites prior signals). Sub-brief split is clean — 288 lands the skeleton independently; 289 strictly extends. Build order: **288 → 289 → close parent 276**. No engine/code changes in this session (docs only). Next step: human disposition → if approved, `/dev-builder` for Brief 288 (consent state machine + decision emails).
- **2026-05-19** — **Designer — Brief 277 (Share Loop & Public Profile Conversion) UX SPEC DRAFTED & fresh-context reviewed PASS-WITH-FLAGS → all 2 must-fix + 6 should-consider folded inline + fresh-context re-reviewer returned CLEAN-PASS; pending human disposition.** Persisted `docs/research/277-share-loop-public-profile-conversion-ux.md` — persona-lens for Rob/Lisa/Jordan/Nadia + four visitor intent shapes (curious peer / similar-expertise / helper-seeker / intro-seeker); six-jobs mapping for member-sharer (DEFINE/DELEGATE/REVIEW/DECIDE/**CURATE**/CAPTURE) and visitor (ORIENT/CAPTURE/DECIDE/DEFINE/DELEGATE/REVIEW/CURATE); Share Studio decomposed as a managed process (8 steps: validate_signal_approved → validate_outcome_consent → generate_variants_per_channel parallel-guarded → render_previews → await_member_edit → record_attribution → capture_referral_context → serve_intent_aware_ctas); four UI surfaces (Studio modal expansion of Brief 260 compact mode; outcome-led consent flow with scrubbed preview + 4-check pass/fail + explicit checkbox gate; visitor CTA strip in `aside` column with intent inference; signed first-party cookie referral preservation); channel × voice compatibility matrix (LinkedIn/X all voices; Instagram loud/ask only; Email/Badge quiet only; PNG/URL voice-N/A); per-channel copy patterns + interaction states (loading-variants / success / error-channel / error-all / outcome-scrub-failed / copied); ContentBlock composition (extend `NetworkProfileCardBlock` with `channelMode`; `AuthorizationRequestBlock` + scrub-check rows for outcome consent; `SuggestionBlock` for visitor CTAs; deferred new `OutcomeConsentBlock`); AC coverage map for all 17 ACs; 8 open questions for the Architect (outcome capture mechanism; Brief 282 scrubber readiness; CTA placement; cookie shape; IG 9:16 card addition; one-call vs per-channel variant gen; domain topology for cookie scope; website badge sandboxing). Fresh-context Dev Reviewer **PASS-WITH-FLAGS** — 2 must-fix (FLAG 1: `ReviewCardBlock` misused with fictional `checks: ScrubCheck[]` field — must replace with `DataBlock` format:list+annotations or `ChecklistBlock`; FLAG 2: Curate-as-7th-job applied as settled when Insight-238 is `active` pending human ratification — spec's "no drift" claim should qualify this dependency) + 6 should-consider (visitor-arrival step belongs to a separate downstream process not inside `share_studio`; `SuggestionBlock` intent metadata should ride `actions[].payload` not new field; Open Q7 domain topology should be elevated to prerequisite — public profile likely lives on `ditto.partners` not `.ditto.you` per project memory, voiding the proposed cookie scope; multi-intent visitor fallback not specified; signal-version coherence for outstanding shares unaddressed; email-sig "Copy HTML" XSS surface needs explicit server-generated+escaped note); strengths called out: process YAML readability, outcome consent rigour, channel×voice matrix, interaction-state granularity, provenance discipline, AC coverage map. Docs-only; no engine/code changes. **Fixes applied inline:** (1) `ReviewCardBlock` swap → `ChecklistBlock` (line 614 of `packages/core/src/content-blocks.ts`) with `{label, status: done|warning, detail?}` per scrub-check row; (2) Curate contingency note added — if Insight-238 declined, outcome-consent reframes as Orient+Decide with no functional change; "no drift" claim qualified for `human-layer.md`; (3) `share_studio` and `visitor_conversion` split into two separate process YAML blocks meeting at the `?ref=` query string interface; (4) `SuggestionBlock` intent metadata moved onto `actions[0].payload` (`ActionDef.payload?: Record<string, unknown>` at line 21) — no new top-level field; (5) **cookie scope corrected to `.ditto.partners`** (verified via `packages/web/app/people/[handle]/page.tsx:59`); cross-domain handoff to `{handle}.ditto.you` documented via signed-query-param re-emission at sign-up (cookies don't cross apex); (6) multi-intent fallback rules added — single-winner (threshold + 0.2 margin) / two-way tie (equal-weight dual highlight) / three-way or noisy (revert to default all-soft) / one-turn decay; (7) signal-version coherence explicitly out-of-scope — share URLs always resolve to current signal, no version pinning (matches every social platform); (8) email-sig "Copy HTML" XSS contract added — server-generated + server-escaped fragment, no `<script>/<style>/<iframe>`, no inline event handlers. Fresh-context re-reviewer **CLEAN-PASS** (all 8 issues resolved, no new issues, document coherence preserved). Next step: human disposition → if approved, `/dev-researcher` (variant-gen LLM prompt branching, cookie/attribution storage, scrubber contract verification, IG 9:16 image gen) and `/dev-architect` for synthesis into the brief's User Experience section.
- **2026-05-19** — **Designer — Brief 275 (Background Watch & Network Health) UX SPEC DRAFTED & fresh-context reviewed PASS-WITH-FLAGS → flags fixed; pending human disposition.** Persisted `docs/research/275-background-watch-network-health-ux.md` (28 sections, the always-on superconnector design) + `docs/research/275-background-watch-network-health-ux-patterns.md` (six-family pattern survey: LinkedIn Job Alerts / Zillow + Redfin saved searches / Notion-Linear-Zapier-Google-Alerts run history / Apple-News-Substack-Stratechery-Discover-Weekly ritual cadence / Pinterest-Goodreads-Spotify implicit feedback / headhunter-buyer-agent restraint). Persona-lens for Rob/Lisa/Jordan/Nadia; six human jobs + Curate (Insight-238) mapped explicitly; full surface inventory A-H plus a B′ multi-watch list view for Jordan; watch lifecycle from creation to fulfillment; Watch Status card with ambient green/amber/red health indicator and ≥3-week amber calibration state; Watch Proposal Queue with "near misses" collapsible (renamed from "held back" per headhunter language); **quiet-state recommendation revised to digest suppression + ambient in-app indicator** (LinkedIn / Google Alerts empirical pattern); digest email anatomy (cap 3, opt-in quiet-week subject line only when user toggled on); 8-rule network-health explainability copy table (Brief 261 Hard Rule #5 anti-persona-non-leakage preserved everywhere); **three-state dismiss** — Not now (90d resurface) / Not a fit (1yr suppress for this watch) / Wrong person entirely (permanent + anti-persona signal) — with optional after-dismiss chip strip; Curate surfaces (refine/pause/resume/close) rooted in Brief 285 Privacy Center; ritual framing copy ("Your Monday network scan"); ContentBlock mapping confirmed (no new schema primitives — disclosure is UI-level inside `watch-proposal-queue.tsx`); 8 D-Qs surfaced for the Architect; 3 open questions for the human (workspace vs Network ownership of cycle YAML; Member Signal opportunity-watch creation surface; cross-watch dedup signals). Fresh-context Dev Reviewer **PASS-with-two-flags + one persona thin spot — all resolved before handoff**: FLAG 1 (§11/§12 quiet-week contradiction → "Quiet week" subject line now explicitly marked opt-in only when the Curate toggle is on, default is no email); FLAG 2 (D-Q6 ambiguity → clarified that the ContentBlock 26-type union constraint is schema-level, UI-level disclosure inside `watch-proposal-queue.tsx` is acceptable); persona thin spot (Jordan's multi-watch list view → added Surface B′ with sort order, grouping, empty state). Verify: docs-only spec; no engine/code changes. Next step: human disposition → if approved, `/dev-researcher` can run in parallel for technical research (scheduler cadence, ranking math, anti-persona signal storage), then `/dev-architect` synthesises both inputs into the brief's User Experience section.
- **2026-05-19** — **Builder — Brief 286 dashboard design recommendations IMPLEMENTED; reviewer-blocker fixes applied; pending human approval.** Converted `/admin/network/superconnector` into a contextual operator inbox + health/audit/suppression workspace with per-band stale notices, structured reveal reasons with server validation, guarded row-owned claim-invite decisions, redacted action-log summaries, and production read-model `decision` metadata for queued/drafted candidates. Verify: focused dashboard/reveal/read-model vitest **18/18**, root `pnpm run type-check` pass, `git diff --check` pass, and Playwright desktop/mobile smoke screenshots at `.context/brief-286-admin-dashboard-*.png`; broad `pnpm test` was not re-run after the blocker fix because the previous broad run was blocked by unrelated `memory-assembly` p95 tripwires.
- **2026-05-19** — **Designer — Brief 276 (Email, Chat, Consent & Introduction Facilitation) UX SPEC DRAFTED & fresh-context reviewed PASS-WITH-FLAGS; pending human disposition.** Persisted `docs/research/276-email-chat-consent-introductions-ux.md` — persona-lens for Rob/Lisa/Jordan/Nadia, six-jobs mapping (Decide primary; Capture/Review/Orient/Delegate; Curate emerging), 5-phase user-facing consent model layered over the brief's 7 technical states, decision-email anatomy per type (requester-approval, recipient-approval, warm intro thread, follow-up), chat refinement surface with the load-bearing "preview-what-they'll-see-before-approve" affordance, natural-language reply ingestion extending `classifyReply` with intro-context categories (approve/decline-not-fit/decline-timing/decline-block/question/refine/outcome-positive/outcome-negative/ambiguous), post-intro outcome taxonomy aligned with Brief 270 paid-successful-outcome pricing roadmap, interaction states per surface, source provenance, and reference-doc status (no drift found). 6 process-architecture recommendations to the Architect (inspectable state machine, email templates as auditable artifacts, email+chat as two surfaces of one decision, cross-deployment delivery for recipient card, voluntary-disclosure outcome capture, marketing-distinguishable copy). 5 open questions for the human (keyword vs LLM classifier; recipient email sender identity; follow-up timing default; decline taxonomy granularity; edit-before-sending impersonation limits). Fresh-context Dev Reviewer **PASS with flags** — 5 strengths, 6 flags (most critical: Flag 1 + Flag 2 — `IntroProposalCard` and `IntroStateLog` introduced as "new chat components" without identifying them as ContentBlock types in the 26-type union, ambiguity must be resolved before Builder can implement; Flag 3 — follow-up sender identity for non-member recipient is internally decided without acknowledging the same deliverability question §15 raises for recipient-approval; Flag 4 — warm-intro no-opt-out assertion deferred to compliance-gate without surfacing as open question; Flag 5 — 5-second-on-mobile litmus not cross-checked against Email Types 3 & 4 (follow-up's two-radio design violates the spec's own one-action rule); Flag 6 — Nadia's aggregate visibility not addressed); 0 failures. Verdict: revise then approve. Next step: human disposition of the 6 flags + 5 open questions → if approved, `/dev-researcher` for technical research (reply classifier, email deliverability, threading semantics) and `/dev-architect` for synthesis into the brief's User Experience section.
- **2026-05-19** — **Builder — Brief 287 (Closeout — review-checklist gates, dictionary, regression, parent-270 acceptance) IMPLEMENTED, per-brief reviewed APPROVE, then closeout-checkpoint reviewer caught a misattribution + a dictionary idempotency violation; both fixed under "fix all". Closeout set (285, 286, 287) ready for fresh-context closeout-checkpoint re-review; pending human approval to close parent Brief 270.** Appended `docs/review-checklist.md` items #20-#26 — the seven durable boolean Network trust gates from Brief 278 D-Q7 (Member Signal provenance, private-leakage scrub coverage, no-contact background watch, two-sided intro consent, claim-before-public discovery, outbound-email suppression/compliance, source-policy enforcement-before-store/outreach), each with a one-line `**Verify:**` note. **Working-tree state at end of session: 26 review-checklist items total** — items #1-#17 from `HEAD`; items #18 (Insight-235 boundary-enforced-by-transport) and #19 (Insight-236 fan-out cross-cutting filter uniformity) **pre-existing in the working tree from the 2026-05-18 Architect reference-doc reconciliation session (which absorbed both insights into review-checklist.md but never committed the diff — see the 2026-05-18 rolling-log entry that explicitly records items 18 and 19 being added that session)**; items #20-#26 added by Brief 287. Append-only constraint maintained — no pre-existing item restructured. When this branch commits, items #18/#19 should be attributed in the commit message to the 2026-05-18 ref-doc reconciliation session, not to Brief 287. Finalized three Brief 270 canonical terms in `docs/dictionary.md` between "Introduction Proposal" and "Network Health" — Invitation Candidate, Discovery Profile, Claim Invite — in lifecycle order. Brief 271's 11 already-finalized terms unchanged (idempotent — no churn after fix-all reverted an in-session expansion of `Possible Connection` that added Brief 274 engine-object detail to a Brief 271-finalized term; the engine-object detail belongs in a follow-on dictionary brief, not 287). Added `pnpm run test:superconnector` to `package.json`: single documented command chaining 8 focused engine vitest files (network-privacy-scrubber, network-audit, network-abuse-controls, discovery-source-policy, network-suppression, network-email-compliance, network-identity-verification, network-retention) + 2 web component test files (privacy-center, network-health-dashboard) + `tsc --noEmit`. Fixed one pre-existing test-fixture omission in `packages/web/components/admin/network-health-dashboard.test.tsx` ("renders row-owned decisions only for active claim-invite rows" was missing the `decision: { kind: "claim_invite_candidate", candidateId }` field the component requires for the mutate-form branch — Brief 286 fixture defect surfaced by the new regression). Brief 285, 286, 278 statuses updated to reference closeout-set readiness. No new product/UI/engine code — docs + script + test fixture only. **Per-brief reviewer APPROVE; fresh-context closeout-checkpoint reviewer (Sonnet, isolated context) initially returned REVISE on two defects: (a) false "items #1-#19 untouched" claim in this rolling-log entry and Brief 287 status line, which misattributed #18/#19 to the Brief 287 session; (b) `Possible Connection` definition expanded in the dictionary diff, violating Brief 287's "idempotent — Brief 271 terms unchanged" constraint. Fix-all applied: this rolling-log entry corrected; `Possible Connection` reverted to Brief 271 form; the seven appended gates and three new lifecycle terms unchanged.** Verify after fix: `pnpm run test:superconnector` 120/120 + type-check 0 errors. Reviewer observation (not a 287 defect): `network-identity-verification.test.ts` has cold-start DB-pool flakiness in the regression suite — recommend a `{ timeout: 15000 }` follow-on for `withNetworkDbTransaction`-based tests; out of 287 scope.
- **2026-05-19** — **Documenter — Brief 284 dev-review + fixes recorded.** Post-implementation `/dev-review` 5-pass audit on the Brief 284 surface returned 6 findings (3 MEDIUM, 3 LOW); user said "fix all" so 5 actionable fixes were applied (1 HIGH localStorage Bearer deferred — inherited `/admin` pattern, not Brief-284-introduced). Fixes: (1) added `system_retention` to `networkAuditEventClassValues` and rewired `network-retention.ts` raw-source-snippet + permanent-stub audits to use it — **closes the non-blocking follow-up flagged in the original Brief 284 entry**; (2) added `VALID_ACTIONS` upfront validation in `/api/v1/network/privacy/{export,delete}` so malformed `action` is rejected with HTTP 400 BEFORE `createNetworkLaneStepRun` — prevents wasted step-run rows and attacker-controlled strings landing in the persisted `route` column; (3) refactored `sweepSoftDeletedSubjects` from per-tombstone O(N·D) full-table re-scan into a scan-once + hash→id `Map` lookup with three `buildXHashIndex` helpers (built only for subject types actually present in the due-tombstones batch); (4) admin superconnector log key changed from mutating `${at}-${idx}` to stable `crypto.randomUUID()` (interface `ActionLog { id: string }`, `appendLog` mints id once); (5) middleware tombstone-fetch doc comment clarified that fail-open is intentional with the page-level `TombstoneFallback` as the authoritative gate (defense-in-depth, not a hole). New insight **Insight-239** captured (validate input shape before minting audited step-runs — refines Insight-180/232). Verify: root `pnpm run type-check` 0 err, `@ditto/core` `tsc --noEmit` 0 err, Brief 284 focused vitest **44/44** (retention + tombstones + privacy/export + privacy/delete routes), admin route vitest **17/17**.
- **2026-05-19** — **Documenter — Brief 279 closeout recorded.** Aligned the brief status, full AC checklist, completion notes, and retrospective with the final fresh-context APPROVE result. State now records the source-row privacy/export/delete/retention fixes, durable send-prepared audit/revocation, and residual non-blocking outbox gap. Roadmap row 279 was already current; the stale Phase 14 status paragraph was reconciled. No new ADR/insight/landscape update needed.
- **2026-05-19** — **Documenter — Brief 285 builder-design review recorded.** `/dev-reviewer` returned **PASS with flags** for the Privacy Center design-recommendation polish: P1 handoff risk from untracked Brief-285 files, P3 missing test assertions for the new rail-state affordance. Reviewer found no privacy-boundary, route, or side-effect regression. Verify rerun: component vitest **6/6** and `pnpm run type-check` pass. No new ADR/insight/landscape update needed.
- **2026-05-19** — **Builder — Brief 285 design recommendations IMPLEMENTED.** Tightened the Privacy Center refinement by calming section status badges, removing the accidental multi-color section taxonomy, and wiring per-section state into the sticky/mobile rail navigation so the provenance ledger communicates operational status at a glance. No backend/privacy-boundary semantics changed. Verify: focused route/component vitest **10/10**, five-file Brief-285 privacy regression vitest **39/39**, `pnpm run type-check`, `git diff --check`, Playwright desktop/mobile screenshots at `.context/privacy-center-builder-*.png`, and browser console smoke with 0 warnings/errors.
- **2026-05-19** — **Designer — Brief 285 (Network Privacy Center) REFINEMENT/ELEVATION PASS complete.** Reworked the member-facing surface from a linear settings stack into a provenance-first privacy ledger: compact trust header, sticky owner rail, indexed section navigation, refined Discovery Profile invitation panel, calmer section/state treatment, tighter empty states, and mobile horizontal section nav. No backend/privacy-boundary semantics changed. Verify: focused route/component vitest **10/10**, five-file Brief-285 privacy regression vitest **39/39**, `pnpm run type-check`, `git diff --check`, and Playwright desktop/mobile screenshots at `.context/privacy-center-refined-*.png`.
- **2026-05-19** — **Designer — Brief 286 post-build UX review COMPLETE & fresh-context reviewed APPROVE WITH NITS; nits fixed.** Persisted `docs/research/286-admin-health-dashboard-postbuild-ux.md`; UX verdict is REVISE for product design but not a backend safety blocker. Main follow-up: convert the form-heavy admin surface into a contextual triage inbox + audit table with per-band states, structured reason taxonomy, stronger provenance, and tighter Precise White Lab visual hierarchy. Docs-only; no implementation code changed.
- **2026-05-19** — **Builder — Brief 279 (Outbound Discovery + Claim Invites) IMPLEMENTED & fresh-context reviewed APPROVE; pending human approval.** Added network discovery schema migration idx 13 (`network_discovery_sources`, `network_discovered_profiles`, `network_discovery_claims`, `network_invitation_candidates`, `network_invitation_events`, `network_claim_tokens`), source registry with LinkedIn pointer-only/scrape hard blocks, guarded `discover_public_people` / `compose_claim_invite` / `send_claim_invite`, internal-only Discovery Profiles, source-backed scoring/suppression, operator queue at `/admin/network/discovery`, token claim route/page with claim/decline/delete, claim-token identity verification, and retention expiry/redaction/export/delete wiring for Discovery Profiles, source rows, and tokens. Review fixes closed LinkedIn snippet persistence, email-bound redemption, claim-token review loading/update, privacy export/delete/retention source coverage, durable send-attempt audit before external email, and prompt/tool-name parity. Verify: focused Brief-279/privacy vitest 161/161 (17 files), adjacent privacy/policy/retention vitest 94/94, root type-check pass, `git diff --check` pass. Reviewer residual: invite send is not a full outbox; process crash after provider acceptance can leave prepared token/event without final sent event.
- **2026-05-19** — **Builder — Brief 286 (Admin health dashboard + dry-run + rate-limit) IMPLEMENTED & fresh-context reviewed APPROVE; pending human approval.** Added `network_rate_counters` migration idx 12 + shared fixed-window `network-abuse-controls.ts` (memory L1 + Postgres backstop, hashed bucket keys, email-challenge outer limiters, suppression-backed operation pause); built `/admin/network/superconnector` dashboard with action queue, subsystem health, aggregate/economic metrics, all-clear state, bounded audit drill, audited raw-text reveal, dry-run no-contact replay, and override endpoint; wired current routes for search/profile-chat/privacy export+delete challenges/share/intro rate limits, share generation + intro approval/decline + public-profile deletion audits, and intro pause enforcement. First review returned REVISE for visitor/profile limiter and request-pause gaps; fixes applied and re-review APPROVE. Dependency caveat: watch-feedback producer/enforcement remains ready for Brief 275's watch surface rather than fabricated. Verify: focused Brief-286 vitest 94/94, root type-check pass, `git diff --check` pass.
- **2026-05-19** — **Builder — Brief 285 (Network Privacy Center member UI) IMPLEMENTED & fresh-context reviewed PASS; pending human approval.** Added standalone `/network/privacy` with the eight-section Privacy Center composed from existing ContentBlock renderers only; added verified-claim-token Discovery Profile entry with four equal exits and provenance-first Original-to-Ditto copy; added chat drill-down link from the Network chat surface. Review fixes close the identity boundary and durable-control gaps: verified fail-closed route boundary, session/context-preserving export/delete, guarded source removal, watch lifecycle, block-list add/remove, profile visibility pause/resume with transactional before/after audit, claim edit/hide/delete, and Discovery Profile claim/decline/suppress/delete exits. Verify: focused Brief-285 vitest **61/61**, claim-invite + handle regression vitest **17/17**, `pnpm --filter @ditto/web test -- privacy`, broad `pnpm test -- privacy` (**3324/3324**, 14 skipped), `pnpm run type-check`, `git diff --check`, and Playwright fail-closed Discovery Profile smoke pass.
- **2026-05-19** — **Builder — Brief 284 (Privacy export/delete + retention + identity verification + admin scaffold) IMPLEMENTED & fresh-context reviewed APPROVE; pending human approval.** Completes the **foundation checkpoint (282+283+284) → unlocks Brief 279**. Added `network_tombstones` table (migration idx 11 `drizzle/network/0011_jittery_morbius.sql`) for hybrid soft-delete + permanent stub identity claims; pluggable identity verifier (`network-identity-verification.ts`: session method + email-challenge slot + claim-token slot, masked-email never echoed); retention engine with ratified defaults (`network-retention-engine.ts`: per-kind windows, hash-based candidate discovery, `system_retention` audits via `operator_approved` class — flagged follow-up); transient identity-gated `/api/v1/network/privacy/export` (R-Q6 — no ArtifactBlock, snapshot returned in response, audit class `privacy_export`); hybrid `/api/v1/network/privacy/delete` (R-Q9 — soft-delete user.status + tombstone row + suppression write in single `networkDb.transaction()`, scheduled hard-purge at +30d, permanent stub at +2y; idempotent re-delete returns 410); `/people/[id]/tombstone-status` JSON probe + middleware-driven HTTP 410 + neutral tombstone page (R-Q11 / Insight-234 #4 anti-resurrection — never re-claimable, never leaks prior identity); admin superconnector scaffold reusing existing `/admin` shell + `requireAdmin` + `isWorkspaceDeployment()` gate — 3 endpoints (`approve` / `suppress` / `pause-discovery`) each minting server-side wrapper runs (Insight-180 + Insight-232/211 caller-stepRunId rejection incl. falsy values matrix `[false, "", null, "network-lane-step:bad"]`). Engine-first: all primitives in `packages/core/src/db/network/schema.ts`. Fresh-context Dev Reviewer APPROVE: all 10 ACs verified (⚠ AC #8 inherits deployment gate from `/admin/layout.tsx` — no direct page-level assertion); 2 non-blocking follow-ups noted (add `system_retention` audit class to `networkAuditEventClassValues`; wire `network-abuse-controls.ts` rate-limiter into `initiateEmailChallenge` as part of Brief 286). Verify: focused Brief-284 vitest **133/133** passing (15 test files), root `pnpm run type-check` **0 errors**; full suite has 2 pre-existing concurrent-DB-isolation flakes (`network-identity-verification`, `network-chat-workflow`) that pass in isolation — not Brief-284 regressions.
- **2026-05-18** — **Documenter — Brief 283 follow-up closeout recorded.** Updated Brief 283 status/AC checklist/completion notes after the review-fix patch, and aligned state/roadmap to the transactional dedup + enforceable suppression uniqueness fixes. No new ADR/insight needed; the durable lesson is captured in the Brief 283 retrospective.
- **2026-05-18** — **Builder — Brief 283 review-fix patch applied.** Fixed `/dev-review` findings by moving complaint webhook dedup claim + complaint handling into one `networkDb.transaction` (handler failure rolls back the dedup claim so Svix retries can reprocess) and replacing nullable-column suppression uniqueness with partial unique indexes (`global`, `per-user`) plus a scope/user check constraint. Verify: focused Brief-283 vitest 93/93, root `pnpm run type-check` pass, `git diff --check` pass. Full `pnpm test` hit 3 unrelated 5s timeout failures under full-suite load; rerunning the exact failed files passed 27/27.
- **2026-05-18** — **Builder — Brief 283 (Source-policy / suppression / email compliance / complaint-pause) IMPLEMENTED & fresh-context reviewed APPROVE.** Added `network_suppressions` + Svix dedup table migration `drizzle/network/0010_slimy_jubilee.sql`, in-code `discovery-source-policy.ts` enforcing collect/store/invite-use gates with audited blocks, hashed `network-suppression.ts` with global/per-user scope + fail-closed reads, `network-email-compliance.ts` with sender identity/RFC 8058/CAN-SPAM/suppression/misleading-subject checks, AgentMail `headers` pass-through in `channel.ts`, and sibling `/api/v1/network/complaints` route with Svix verification, caller-stepRun rejection, server-minted wrapper run, svix-id dedup, complaint suppression, per-event audited complaint counters, and source/segment auto-pause. First review returned REVISE for complaint counters tied to suppression-state rows; fixed by counting per-event complaint audit rows after Svix dedup, preserving suppression idempotency while distinct complaints still trip pause. Verify: focused Brief-283 vitest 90/90, root + web type-check pass, full `pnpm test` 3122/3122 (14 skipped), integration spike present but skipped locally without live AgentMail env, fresh-context re-review APPROVE.
- **2026-05-18** — **Builder — Brief 282 (Network audit / scrubber / stop-run substrate) IMPLEMENTED & fresh-context reviewed APPROVE; pending human approval.** Added generic append-only `network_audit_events` with migration `drizzle/network/0009_daffy_sir_ram.sql`, server-minted wrapper-run audit validation via the `createNetworkLaneStepRun` JSONL log, central pure `scrubForSurface` over all 8 leakage surfaces, and block/share/OG safeguards so `NetworkProfileCardBlock.antiPersonaMd` is owner-only. First review returned REVISE for fabricated UUID-shaped audit IDs + direct silhouette anti-persona rendering; fixed and re-reviewed APPROVE. Follow-up `/dev-review` returned REVISE for `generateShareVariants` accepting fabricated step IDs + missing UUID-shaped share-route bypass regression; fixed by requiring server-minted Network wrapper runs before completion and adding route coverage, then re-reviewed APPROVE. Verify: focused vitest 47/47, `pnpm run type-check` pass, full `pnpm test` 3065/3065 (13 skipped), `git diff --check` pass, `pnpm test:e2e:auto` pass; broad `pnpm test:e2e` still has unrelated baseline failures (25 passed / 17 failed).
- **2026-05-18** — **Architect — Brief 278 (Trust, Privacy, Admin, Observability) DESIGN COMPLETE & fresh-context reviewed (APPROVE WITH MINOR REVISIONS → all 2 blocking + 8 non-blocking fixes applied); pending human approve/revise/reject.** Parent **Brief 278** rewritten end-to-end as the **trust/privacy contract for the Network Superconnector program**, then split per Insight-004 into a **foundation checkpoint** (sub-briefs **282 / 283 / 284**) and a **closeout checkpoint** (sub-briefs **285 / 286 / 287**), each independently testable. 17 design questions answered (D-Q1…Q17) + ratification of 10 inherited Research-278 open Qs + 1 retention default pending human ratify. Cross-cutting decisions: (a) **Insight-238 ruling — Curate is the 7th human job** (Privacy Center is the first full realisation; new insight `docs/insights/238-curate-is-the-seventh-human-job.md` flagged for human ratification; no new ContentBlock needed); (b) **AgentMail compliance** = extend `channel.ts AgentMailAdapter` per Researcher Q5 resolution (RFC 8058 `List-Unsubscribe`/-Post via `headers`, typed `Complaint`); (c) **complaint webhook** = new sibling route `packages/web/app/api/v1/network/complaints/route.ts` (NOT `/inbound` extension — Brief 098b stays MessageReceived-only); Svix verification + `svix-msg-id` dedup + server-side wrapper run minted via `createNetworkLaneStepRun` (Insight-232 + Insight-215 internal-DB-only regime — no fabricated "webhook exception" precedent); (d) **fixed-window rate-limit** algorithm committed (single Postgres write per bucket per window); (e) **Hard Rule #5 origin** corrected to Brief **259** system prompt (carried forward by 261) across all 6 affected briefs; (f) **3 new `docs/review-checklist.md` Network trust gates enumerated** (member-signal provenance, private-leak scrub coverage, no-contact background watch, two-sided intro consent, claim-before-public discovery, outbound-email suppression/compliance, source-policy enforcement-before-store/outreach — assigned to closeout brief 287); (g) **§Cross-Cutting Risks** section added to parent (6 risks each mapped to owning sub-brief). Build order: **282 → 283 → 284 (foundation checkpoint → 279 production discovery) → … → 285 + 286 → 287 (closeout checkpoint → close parent 270)**. Reviewer fixes applied: B1 (replaced fabricated webhook-exception precedent with HTTP-route wrapper-run framing), B2 (committed sibling route + dedup + handler split), N1 (AC#5 expanded into 5a-5e individually testable sub-criteria), N2 (Researcher Q5 cited explicitly), N3 (Hard Rule #5 citation fixed), N4 (282 `connection-proposal.ts` description = "follow as precedent", not modify), N5 (added "reserved `prevHash` rationale" + scrubber-bypass discipline in 282/286), N6 (Insight-238 §Resolution subsection), N7 (parent §Constraints clarified Insight-215 split), N8 (Cross-Cutting Risks parent section + per-sub-brief constraints for 282/283/284/286 covering all 6 sub-risks). **Next pipeline step: human approval, then `/dev-builder` for sub-brief 282 (Audit / Scrubber / Stop-run substrate).**
- **2026-05-18** — **Research-278 Open-Question Q5 (AgentMail compliance capability) RESOLVED by Researcher verification.** Pinned SDK `agentmail@0.4.18` confirms: `SendMessageRequest`/`ReplyToMessageRequest` accept `headers: Record<string,string>` (RFC 8058 `List-Unsubscribe`/`-Post` injectable on send **and** reply); webhook `EventType` includes `message.complained` (typed `Complaint{recipients[],type,subType}`) + delivered/sent/rejected/spam — not only received/bounced; webhooks Svix-signed and Ditto **already verifies** them in `network/inbound/route.ts`. Effect: gap #5 (email-compliance) downgrades Original-to-Ditto→**extend** of `channel.ts` `AgentMailAdapter`; gap #9 ingestion path **closed** (only complaint→suppression handler + threshold circuit-breaker remain Original-to-Ditto). `docs/research/278-trust-privacy-admin.md` §3/§4/§11/§15/§16/§17/§18 + `docs/landscape.md` AgentMail entry rewritten to the verified fact. Architect now inherits **10** open Qs (Q1–Q4, Q6–Q11), not 11.
- **2026-05-18** — **Research-278 (Trust, Privacy, Admin & Observability technical precursors) COMPLETE & fresh-context reviewed (PASS WITH FLAGS → all 3 flags + 3 NITs fixed); pending human approve/revise/reject.** Dev Researcher per-work-product build-FROM inventory for Brief 278's 13 work products: the `stepRunId` wrapper-run pattern (`packages/web/app/api/v1/network/search/route.ts`), proposal/credential scrub precedents, three Network-tier append-only audit tables, `visitor-rate-limit.ts`/`turnstile.ts`/`networkUserBlockList`, `requireAdmin`+deployment-mode-gate admin auth, and migration idx **9** are reuse/extend; **12 items flagged Original to Ditto** (general PII scrubber, DSAR export/delete+tombstone, claim-token identity, email-compliance wrapper, suppression list, source-policy 3-point enforcement, ARF/FBL complaint-pause, privacy/safety audit surface, multi-instance rate limiting). External pattern layer: CAN-SPAM/RFC 8058, GDPR/CCPA windows, RFC 9309 robots, rate-limit families, circuit-breaker 3-state, tamper-evidence ladder. **11 Open Questions surfaced for the Architect.** Report `docs/research/278-trust-privacy-admin.md` (companion to Designer `-ux.md`); `docs/landscape.md` (+`rate-limiter-flexible`/`@upstash/ratelimit`, OPA/Rego pattern-ref, AgentMail compliance-capability annotation) + `docs/research/README.md` index updated. Reviewer FLAG-1 (export-artifact neutrality rebalanced), FLAG-2 (retention-window Architect Q11 added), FLAG-3 (Brief 261 full path verified), NIT-1/2/3 (scrub.ts L37–94 / opt_out L64 / scrubProposalText L104–120 + privateValues L98–101) all fixed. Next pipeline step: `/dev-architect`.
- **2026-05-18** — **Architect reference-doc reconciliation pass COMPLETE (docs-only, fresh-context reviewed PASS-WITH-FLAGS → all 4 flags fixed).** Realigned `docs/human-layer.md` + `docs/architecture.md` to the post-Brief-280/281 Workspace IA (single `/chat` Self conversation home; three-panel shell deleted; artifacts inline; Archive-drawer recall; legacy pages = drill-downs) and absorbed **Insight-235** (front-door/workspace boundary enforced by transport, not a `/chat` runtime filter — added to architecture.md action-boundaries + a `docs/dev-process.md` brief-writing "Boundary-AC enforcement-seam rule" + `docs/review-checklist.md` item 18) and **Insight-236** (shared multi-kind recall helper resolves a cross-cutting filter once — added to architecture.md L4/L6 recall-primitive paragraph + `docs/review-checklist.md` item 19). Reviewer caught 3 residual stale "sidebar" references (Brief-073 composition-engine paragraphs + Workspace Compositions table) + 1 minor `recallWorkspace()` provenance nit; all fixed (now cite `src/engine/workspace-recall.ts`). human-layer.md + architecture.md grep-clean of contradictory present-tense panel references. Closes the "Reference-doc drift — Briefs 280 + 281" Known Gap.
- **2026-05-18** — **Brief 273 (Need Request onboarding + manual-search entry) COMPLETE & APPROVED.** Fresh-context review found two blockers plus verification drift: identity collected in the request UI was not persisted, `update_need_request` trusted model-supplied owner ids, repeated saves could duplicate Active Requests, seeded handoff had a 45s delay/e2e failure, and the full suite still had unrelated baseline drift. All fixed under "fix all": request save now sends `identity` + `requestId` and updates existing rows; `update_need_request` derives actor ownership from execution context and exposes no owner-id fields in its model schema; raw-need LLM drafting has explicit untrusted-input boundaries; seeded request analysis minimum is 1.8s; `/network` handoff is deterministic; stale Brief-274 `@ditto/core/content-blocks` import fixed; baseline test drift closed by completing the provisioning mock and isolating network-tier tests with `withNetworkDbTransaction`. Verify: focused Brief-273 vitest 96/96, landing Playwright 8/8, root + web type-check 0 err, full `pnpm test` 3037/3037 (13 skipped), `git diff --check` clean. Moved to `docs/briefs/complete/`.
- **2026-05-18** — **Brief 274 (Manual Search & Possible Connection proposals) COMPLETE & APPROVED.** After the REVISE was completed (search UI wired into the client lane + `jobRequestCard` scrub-passthrough + 2 route tests + AC#18 Playwright spec), a focused fresh-context Dev Reviewer re-reviewed the **wiring only** (per the first reviewer's explicit recommendation — engine/data layer was already independently re-verified 9 PASS / 3 FLAG / 0 FAIL) and returned **APPROVE-WITH-NITS**: ACs 1/2/3/13 reachable end-to-end, AC#18 runs+passes against the real app, Insight-180/232 intact and tested, no regression (107/107), scope clean (Brief 280/281 worktree files pre-existing parallel-stream, not introduced here). Three nits dispositioned under "fix all": **nit 1 FIXED** (the two adjacent identical-gate `currentMode === "client" && clientMatchCard` sibling render blocks merged into one shared-gate block); **nit 2 correct no-op** (`jobRequestCardOrUndefined()` structural-only validation is deliberate — the engine private-fact scrub is the real boundary; deep field validation here would duplicate it, the exact anti-pattern Insight-235 warns against); **nit 3 correct no-op** (forwarding `reasonText`/`refinementText` would require a free-text reason UI the brief never specced — out-of-brief scope creep; PATCH route already accepts them as optional for a future brief). Post-fix verify: core + root type-check 0 err; search-route + manual-search vitest 19/19; network components + chat regression 107/107; Playwright AC#18 1/1. Human-approved.
- **2026-05-18** — **Brief 281 (Workspace Artifact Recall & Archive) COMPLETE & APPROVED.** After the maker-checker APPROVE-WITH-NITS (16/16 ACs PASS) a `/dev-review` 5-pass audit surfaced 7 findings; all fixed under "fix all": (1) memory process→project resolution N+1 batched into two `inArray` queries + lookup maps; (2) a single `ProjectFilter` resolved once in `recallWorkspace` and threaded into every collector (unresolved slug short-circuits to `[]`, non-project-scopable kinds omitted under a project filter) — replaces the inconsistent per-kind handling; (3) real focus trap + focus restore in `archive-drawer.tsx` (capture `activeElement` on open, Tab cycling within the panel, restore on close); (4) memory `memoryScopeType`/`memoryType` threaded through `RecallResult` → `self-stream.ts` `knowledge_citation` sources for scope-pill fidelity; (5) `inArray`/`and` imports corrected; (6) `" none"` projectSlug sentinel removed; (7) ARIA corrected to semantic `list → listitem → link` (non-routable rows are `aria-disabled` listitems, never invented links). Verify: root + web type-check clean for Brief-281 files, focused vitest 194/194 (8 files), Playwright `workspace-artifact-recall.spec.ts` 3/3. Insight-236 captured. The remaining web tsc `@ditto/core/content-blocks` error is a parallel Brief-274 file — out of 281 scope.
- **2026-05-16** — **Brief 281 (Workspace Artifact Recall & Archive) implemented + reviewed — APPROVE-WITH-NITS.** Shared read-only `src/engine/workspace-recall.ts` (`recallWorkspace()`) over projects/processes/memories/work/reviews/recent-activity; real route shapes only. Consumed by workspace-gated `search_workspace` Self tool (inline Alert/KnowledgeCitation/Record/InteractiveTable blocks) + read-only authed `GET /api/v1/workspace/archive` backing the chat-header Archive drawer (`archive-drawer.tsx` + `page.tsx`/ChatNav wiring). Insight-211/183/235 honored; zero mutation paths (route exports GET only). Verify: root+web type-check 0 err; focused vitest 190/190; Playwright `workspace-artifact-recall.spec.ts` 3/3; pre-existing unrelated suite failures confirmed on baseline via stash. Fresh-context Dev Reviewer independently re-verified (16/16 ACs PASS, 12-pt checklist clean): 1 SHOULD-FIX scope question (`search_knowledge` ingested-doc store out of recall scope — memory-only defensible; PM/Architect confirm) + 3 NITs, no blockers. Pending human decision.
- **2026-05-16** — **Brief 274 (Manual Search & Possible Connection proposals) implemented.** New engine modules (`connection-proposal.ts`, `network-manual-search.ts`, `network-search-feedback.ts`) + network schema migration `0008_smooth_leo.sql` (4 tables: search runs / possible connections / search feedback / search audit events). Possible Connection = reasoned engine object (why/whyNow/evidence+provenance/risks/confidence/consent-aware introEligibility/nextAction/scrubApplied), not a marketplace card. `run_network_search` + `record_network_search_feedback` tools registered (Insight-180 step-run guard); audited HTTP route `/api/v1/network/search` (Insight-232 caller-stepRunId rejection incl. falsy). UI: `PossibleConnectionCard` / `SearchResultsPanel` / `SearchBox`; `suggested-candidates-panel.tsx` additively extended (no regression). Private budget/anti-persona scrub enforced in seeker copy. First fresh-context Dev Reviewer verdict was **REVISE** — engine/schema/audit/scrub/step-run/route layer independently re-verified high-quality (9 PASS / 3 FLAG / 0 FAIL; safety gates pass), but one blocking integration gap: search UI was built + unit-tested yet not mounted in any rendered page (ACs 1/2/3/13 fail end-to-end, AC #18 unrunnable). **REVISE completed ("boil the ocean"):** (1) `network-chat-shell.tsx` now mounts `SearchBox` (grounded `from-request`, save-checkbox hidden) + `SearchResultsPanel` in the client lane gated on `currentMode === "client" && clientMatchCard`, with `runManualSearch` (POST `/api/v1/network/search`) and `recordConnectionAction` (PATCH, optimistic hide/not-a-fit filtering) handlers; (2) route `POST` now validates+forwards `jobRequestCard` via `jobRequestCardOrUndefined()` — closes a genuine wiring defect where Active-Request-grounded search silently dropped the card and never engaged the private-fact scrub; (3) 2 new route tests (forwards valid card / drops non-card junk); (4) new AC#18 Playwright spec `network-manual-search.spec.ts` (completes the 6-question client brief, runs a grounded search, asserts honest panel outcome + no crash; e2e env has no Network tier — documented Known Gap — so the spec proves mounting/integration/graceful-degradation while `search-results-panel.test.tsx` owns the per-state matrix). Verify (revise): core + root type-check 0 err; search-route + manual-search vitest 19/19; network components + chat regression 107/107; Playwright AC#18 1/1. → closed & approved 2026-05-18 (see top entry).
- **2026-05-16** — **Brief 280 (Workspace Conversational Front Door / Self-First IA) COMPLETE & APPROVED.** `/chat` is now the authenticated post-Day-Zero workspace Self home (drives AI SDK `useChat` against `/api/chat`/`selfConverseStream`); processes/reviews/work/briefings/progress render inline as existing ContentBlocks; legacy primitive pages remain drill-downs. Maker-checker review APPROVE-WITH-NITS; AC8 trust-tier-label fix applied + regression-pinned; Insight-235 captured (boundary enforced by transport ≠ runtime filter); orphaned `workspace-page.tsx` deleted. Verify: root type-check 0 err, focused vitest 74/74, Playwright onboarding + workspace-chat-front-door 6/6. + state.md compaction this session.
- **2026-05-15** — Brief 272 (Member Signal Onboarding/Research/Provenance) closed: PASS with 2 non-blocking flags (pre-existing test baseline drift; Brief-273 orphan migration snapshot).
- **2026-05-14** — Brief 273 (Need Request onboarding + manual-search entry) built; fresh-context review was initially blocked by usage limit, then closed complete + approved on 2026-05-18. Brief 271 (Network Doctrine/IA/Superconnector copy) implemented, Reviewer flags resolved, pending human approval.
- **2026-05-13** — Parent Brief 254 loop (Network two-sided front door, sub-briefs 255–261) built through 261; pending re-review/approval. Task #18 launchpath workspace re-provisioned via Brief-267-hardened provisioner; healthz middleware hotfix shipped (PR #52/#53).
- **Earlier** — Briefs 267 (provisioner hardening), 268+269 (managed-workspace onboarding + bridge distribution), parent-254 sub-briefs. See git history for full per-session log.

## What's Working (capability snapshot — architecture.md is the design source of truth)

- **Web app** — Next.js 15 App Router (`packages/web/`), pnpm monorepo. Self streaming via `selfConverseStream()` (5 LLM providers) over `/api/chat` (AI SDK `createUIMessageStream`) + SSE `/api/events`. 22-type ContentBlock protocol with unified block registry. AI Elements conversation UI (`useChat` full surface: dataPartSchemas, throttle, stop/regenerate, tool-approval, onData/onFinish).
- **Workspace IA (Brief 280)** — authenticated post-Day-Zero users land in `/chat`, the workspace Self surface. Artifacts (process/review/work/briefing/progress) render inline as ContentBlocks; legacy primitive pages are drill-downs. Day Zero gate, workspace auth, action boundaries, Self-guidance parity, side-effect guards preserved. Front-door restriction is transport-level (Network engine), not a runtime filter on `/api/chat` — see Insight-235.
- **Engine core (`@ditto/core`)** — extracted reusable harness package. Schema, harness pipeline, trust system, LLM types, cognitive framework, content blocks, engine interfaces. `src/engine/` holds Ditto product layer (Self, system agents, self-tools, network, personas); 8 thin re-export files bridge to core.
- **Storage** — SQLite + Drizzle + better-sqlite3, WAL. Schema split by ADR-025 deployment topology (engine/network/frontdoor/product). Drizzle Kit migrations; `ensureSchema()` runs migrator at startup. Network tier on dedicated Supabase project (Tokyo).
- **Harness** — 13-handler pipeline; 4-tier trust gate (deterministic SHA-256 sampling, session/step-category overrides, broadcast forcing); trust earning (sliding window); review patterns (maker-checker/adversarial/spec-testing); harness events.
- **Conversational Self** — persistent identity, tiered context, `selfConverse()` tool-use loop, sessions (DB-backed, cross-surface resumption, inbound scoped separately). ~26 self-tools (dev roles, pipeline, work items, process gen, briefing, risk, confidence). Proactive engine (briefing/risk/suggest-next). Onboarding process + `adapt_process`. Cognitive framework (`core.md` + `self.md`).
- **Processes / integrations** — YAML process loader (parallel groups, deps, routing, retry). Integration registry (CLI + REST handlers), credential vault (AES-256-GCM), process I/O (polling triggers + output delivery), integration generation from OpenAPI. Agent tools (read/write/exec, path+secret+symlink guards, allowlisted `run_command`).
- **System agents** — trust-evaluator, intake-classifier, router, orchestrator (goal decomposition), process-validator. Run through the harness pipeline.
- **Network Agent (parent-254 chain)** — two-sided conversational front door: `/network` landing, expert/client/visitor lanes, `NetworkProfileCardBlock`/`JobRequestCardBlock`, on-network match, `/people/[handle]` profile-as-chat, share modal + dynamic OG/PNG, introductions table + free counter + workspace upsell. Member Signal onboarding/research/provenance (Brief 272). **Active Requests (Brief 273)** — need-first `/network/request` onboarding drafts one-line needs into editable outcome/proof/geography/commercial/private fields, persists calibrated identity, creates search/watch handoffs in evolved `networkJobRequests`, rejects caller `stepRunId`, and lets users choose manual search/background watch/both without full profile setup. **Manual Search & Possible Connections (Brief 274)** — evidence-backed `runNetworkSearch` returning reasoned Possible Connection objects (why/whyNow/evidence+provenance/risks/confidence/consent-aware introEligibility/nextAction/scrubApplied; never a marketplace list, never contacts anyone before consent); audited `/api/v1/network/search` POST/PATCH (Insight-180 step-run guard, Insight-232 caller-stepRunId rejection); grounded `SearchBox`/`SearchResultsPanel`/`PossibleConnectionCard` mounted in the client lane; private budget/anti-persona scrub when grounded in the brief. **Trust/privacy substrate (Brief 282)** — generic append-only `network_audit_events`, server-minted wrapper-run audit writer, central eight-surface privacy scrubber, and block-level anti-persona owner-only rendering. **Outbound safety gate (Brief 283)** — source-policy enforcement, hashed suppression list, email-compliance/RFC 8058 helper, AgentMail header pass-through, and complaint webhook with Svix dedup + complaint-triggered source/segment pause are implemented and reviewed. **Privacy contract + admin scaffold (Brief 284)** — pluggable identity verifier (session/email-challenge/claim-token), transient identity-gated DSAR export (`privacy_export` audit, no ArtifactBlock), hybrid soft-delete + tombstone + suppression in one transaction with scheduled hard-purge at +30d / permanent stub at +2y, HTTP 410 + neutral tombstone page for deleted profiles (Insight-234 #4 anti-resurrection), retention engine with ratified per-kind defaults, and admin superconnector scaffold (`approve` / `suppress` / `pause-discovery`) reusing `/admin` + `requireAdmin` + `isWorkspaceDeployment()` — caller-stepRunId rejection enforced on every POST seam. **Outbound Discovery + Claim Invites (Brief 279)** — public/source-registry discovery with LinkedIn pointer-only/scrape hard blocks, internal-only Discovery Profiles, source-backed claim scoring/suppression, admin operator queue, approved claim invites, hashed 30-day claim tokens, and claim/decline/delete flow into editable Member Signal review.
- **Dev pipeline** — 7 dev roles as skills, `claude -p` orchestrator + Telegram bot, engine-integrated via `processes/dev-pipeline.yaml`. Distributed knowledge maintenance (Insight-043).
- **Test infra** — vitest unit suite + Playwright e2e (`MOCK_LLM=true`, page objects, DB reset). Real SQLite per test via migrations.
- **Deployment** — Railway (not fly.io — stale `fly.toml`). ditto.partners = Network/front door; ditto.you = per-user workspace face. launchpath managed workspace live.

## In Progress

- **Brief 278 (Trust, Privacy, Admin, Observability) closeout checkpoint — Briefs 285 + 286 + 287 all IMPLEMENTED & per-brief reviewed PASS/APPROVE.** Foundation sub-briefs 282+283+284 green; Brief 279 implemented + reviewed APPROVE. Closeout set is ready for the human-triggered fresh-context **closeout-checkpoint review** (parent Brief 278 §Review Process step 3) spanning Briefs 270-279 + sub-briefs 282-287 + architecture.md + the review-checklist (with the new gates) + all implemented diffs. After that review and human approval, parent Brief 270 closes. Brief 286 leaves watch-specific producer/enforcement as a dependency gap for Brief 275 rather than fabricating a non-existent watch surface.

## What's Blocked

- _(none known in the current approved Network Superconnector chain; PM should sequence the next brief.)_

## Recently Completed (current cycle)

- **Brief 287** — Closeout (review-checklist gates + dictionary + regression + parent-270 acceptance) — IMPLEMENTED & per-brief reviewed APPROVE 2026-05-19; closeout-checkpoint reviewer caught misattribution of pre-existing items #18/#19 + in-session expansion of `Possible Connection`; both fixed under "fix all" (rolling-log entry corrected, `Possible Connection` reverted to Brief 271 form). Closeout set (285, 286, 287) ready for fresh-context closeout-checkpoint re-review; pending human approval to close parent Brief 270. Adds 7 durable boolean Network trust gates to `docs/review-checklist.md` (#20-#26 — items #18/#19 pre-existing from 2026-05-18 ref-doc reconciliation session, uncommitted), 3 finalized Brief 270 canonical terms to `docs/dictionary.md` (Invitation Candidate, Discovery Profile, Claim Invite — Brief 271 terms strictly unchanged after fix-all), and `pnpm run test:superconnector` single-command regression (120/120 + type-check 0 errors). Also fixed a pre-existing Brief 286 test-fixture omission surfaced by the new regression. No new engine/product/UI code.
- **Brief 285** — Network Privacy Center member UI — IMPLEMENTED & fresh-context reviewed PASS 2026-05-19; pending human approval; Designer refinement/elevation pass complete and Builder design-recommendation polish implemented; polish review PASS with flags recorded (untracked-file handoff risk + rail-state test coverage). Focused vitest 61/61 plus refinement/builder route/component 10/10 and five-file regression 39/39, broad `pnpm test -- privacy` 3324/3324, root type-check pass, Playwright smoke/screenshots pass.
- **Brief 286** — Admin health dashboard + dry-run + rate-limit — IMPLEMENTED & fresh-context reviewed APPROVE 2026-05-19; pending human approval. Shared rate-limit and operation-pause substrate, admin dashboard/dry-run/reveal/override routes, bounded-visibility tests, current route integrations for search/profile-chat/privacy challenge/share/intro; post-build design recommendations now implemented with contextual inbox/audit layout, structured reveal reasons, guarded claim-invite decision metadata, and stale-data band notices. Post-implementation `/dev-reviewer` fix-all closed six findings (workspace-mode admin API hard-404, pause-protected `sendNow`/engine sendability, fail-closed pause UI, true action-required totals, structured reason enforcement, broad anti-persona scrub) and fresh-context re-review returned APPROVE.
- **Brief 279** — Outbound Discovery and Claim Invites — IMPLEMENTED & fresh-context reviewed APPROVE 2026-05-19; pending human approval. Source registry, internal Discovery Profiles, claim-token flow, guarded discovery/compose/send tools, operator queue, claim-token identity verification, source-row privacy export/delete/purge, retention wiring, and durable send-prepared audit/revocation. Residual follow-up: not a full outbox after provider acceptance.
- **Brief 284** — Privacy export/delete + retention + identity verification + admin scaffold — IMPLEMENTED & fresh-context reviewed APPROVE 2026-05-19. Completes foundation checkpoint 282+283+284 → unlocks Brief 279. Focused vitest 133/133, root type-check 0 errors.
- **Brief 283** — Source-policy / suppression / email compliance + complaint webhook — IMPLEMENTED & fresh-context reviewed APPROVE 2026-05-18; follow-up review patch fixed transactional webhook dedup rollback and enforceable global suppression uniqueness (focused vitest 93/93; root type-check pass).
- **Brief 274** — Manual Search & Possible Connection proposals — COMPLETE & APPROVED 2026-05-18 (REVISE completed, wiring-only re-review APPROVE-WITH-NITS, nit-1 fixed; vitest 19/19 + 107/107, Playwright AC#18 1/1).
- **Brief 281** — Workspace Artifact Recall & Archive — COMPLETE & APPROVED 2026-05-18 (maker-checker APPROVE-WITH-NITS + `/dev-review` 7 findings all fixed; vitest 194/194, Playwright 3/3).
- **Brief 273** — Need Request onboarding + manual-search entry — COMPLETE & APPROVED 2026-05-18 (review blockers fixed; full `pnpm test` now 3037/3037).
- **Brief 280** — Workspace Conversational Front Door / Self-First IA — COMPLETE & APPROVED 2026-05-16.
- **Brief 272** — Member Signal Onboarding, Research, Provenance — closed PASS with 2 non-blocking flags, 2026-05-15.
- **Brief 271** — Network Doctrine, IA, Superconnector Copy — implemented, Reviewer flags resolved, pending human approval.
- **Parent Brief 254** (sub-briefs 255–261) — Network two-sided conversational front door — built through 261; closeout pending re-review/approval.
- **Briefs 267 / 268 / 269** — provisioner hardening + healthz hotfix; managed-workspace onboarding + bridge distribution.

## Known Gaps & Debt (live only)

- **Vision / image analysis** — HIGH. `content.analyze_image` not built. Scoped: Brief 207 (draft, builder-claimable, no deps).
- **Slack adapter + channel tool surface** — MEDIUM-HIGH. Scoped: Brief 208 (draft, builder-claimable, parallelizable with 207).
- **Sandboxed code execution** — deferred behind named trigger (first `requires: code_execution` spec / explicit user ask / ≥2 process-gen outputs needing it). Decision owner: Architect → new ADR.
- **Brief 074 trust-gate goal-override** — *design gap, not doc drift*: trust-gate handler does not read `goalTrustOverrides` from processRun inputs (stored + logged, not enforced). Needs Architect session. Session-level overrides are implemented.
- **Test baseline drift** — ~~`network-chat*` suites fail when `SUPABASE_DB_URL`/`network-db` unset; `inbound-email.test.ts` missing `provisioningErrorMessage` mock export~~ **RESOLVED 2026-05-18** during Brief-273 review-fix closeout. Network-tier tests now inject `withNetworkDbTransaction`; the deterministic focused suites pass.
- **Full-suite load timeouts** — LOW. Brief-283 follow-up `pnpm test` hit three 5s timeout failures under full-suite load (`network-match`, work-item status route); rerunning those exact files passed 27/27. Treat as timing pressure to monitor, not a product blocker.
- **Code debt** — test-utils `createTables` SQL must be hand-synced to Drizzle schema; `src/dev-session.ts` duplicate `DATA_DIR` should import `paths.ts`. Tracked in `docs/debts/` (`pnpm cli debt`).
- **Reference-doc drift — Briefs 280 + 281** — ~~design docs still describe the deleted three-panel shell as the home; Insight-235/236 unabsorbed~~ **RESOLVED 2026-05-18** (Architect reconciliation pass, fresh-context reviewed PASS-WITH-FLAGS, all 4 flags fixed). `docs/human-layer.md` + `docs/architecture.md` realigned to the post-280/281 single-`/chat` IA (grep-clean of contradictory present-tense panel references; legitimate historical/negation references retained); Insight-235 → architecture.md action-boundaries + `docs/dev-process.md` brief-writing rule + review-checklist.md item 18; Insight-236 → architecture.md L4/L6 recall primitive + review-checklist.md item 19. Documenter: verify the rolling-log entry and run retro at session end.
- **Doc hygiene** — ~~state.md 4156-line bloat~~ **RESOLVED 2026-05-16** (this compaction; was 4156 → 81 lines, full history in git, phase milestone curated to `docs/changelog.md`). Maintain compact going forward (rolling one-line log, no narrative blocks).

## Active Briefs (forward / in-flight only — full backlog in docs/roadmap.md + docs/briefs/)

| Brief | Status |
|-------|--------|
| 281 — Workspace Artifact Recall & Archive | **COMPLETE & APPROVED 2026-05-18** (maker-checker + `/dev-review` 7 findings fixed). Moved to `docs/briefs/complete/`. |
| 273 — Need Request onboarding + manual-search | **COMPLETE & APPROVED 2026-05-18.** Review blockers fixed; moved to `docs/briefs/complete/`. |
| 274 — Manual Search & Possible Connection proposals | **COMPLETE & APPROVED 2026-05-18.** Wiring-only fresh-context re-review APPROVE-WITH-NITS; nit-1 fixed (merged duplicate-gate render blocks), nits 2-3 correct no-ops (Insight-235 / out-of-brief scope). All checks green. |
| **278** (parent) — Trust, Privacy, Admin, Observability | **Architect design COMPLETE & fresh-context reviewed (APPROVE-WITH-MINOR-REVISIONS → all B1+B2+N1–N8 fixes applied) 2026-05-18.** Split per Insight-004 into foundation (282/283/284) + closeout (285/286/287). Pending human approval. |
| **282** — Network audit / scrubber / stop-run substrate | **IMPLEMENTED & fresh-context reviewed APPROVE 2026-05-18; pending human approval.** Adds `network_audit_events`, migration idx 9, server-minted wrapper-run guard, central scrubber, and anti-persona block/share/OG safeguards. |
| **283** — Source-policy enforcement / suppression / email compliance + complaint webhook | **IMPLEMENTED & fresh-context reviewed APPROVE 2026-05-18; follow-up review-fix patch applied + documented.** Adds source-policy registry, `network_suppressions`, email-compliance helper, AgentMail RFC 8058 headers, sibling complaint route, transactional svix-id dedup, and complaint-triggered source/segment pause. |
| **284** — Privacy export/delete + retention + identity verification + admin scaffold | **IMPLEMENTED & fresh-context reviewed APPROVE 2026-05-19; pending human approval. Post-implementation `/dev-review` 5-pass + fix-all closed both follow-ups 2026-05-19.** Adds `network_tombstones` (migration idx 11), pluggable identity verifier, retention engine, transient identity-gated `/api/v1/network/privacy/export`, hybrid soft-delete + tombstone `/api/v1/network/privacy/delete` (single transaction + scheduled purge), `/people/[id]/tombstone-status` + middleware 410 + neutral tombstone page (Insight-234 #4), and admin superconnector scaffold (approve/suppress/pause-discovery) reusing `/admin` + `requireAdmin` + `isWorkspaceDeployment()`. Polish pass added `system_retention` audit class, upfront `VALID_ACTIONS` rejection before step-run mint (Insight-239), retention-sweep hash-index refactor, stable React keys, middleware fail-open doc. **Completes foundation checkpoint → unlocks Brief 279.** |
| **285** — Network Privacy Center (member UI — Curate's first full realisation) | **IMPLEMENTED & fresh-context reviewed PASS 2026-05-19; Designer refinement/elevation pass complete; Builder design-recommendation polish implemented; polish review PASS with flags recorded; pending human approval.** Standalone `/network/privacy`, verified-identity fail-closed route boundary, chat drill-down, eight-section existing-block composition, verified Discovery Profile four equal exits, guarded privacy controls, privacy leakage tests, provenance-first ledger UI with per-section rail status. Follow-ups before PR/handoff: ensure untracked Brief-285 files are included and add rail-state assertions. |
| **286** — Admin health dashboard + dry-run + rate-limit | **IMPLEMENTED & fresh-context reviewed APPROVE 2026-05-19; post-implementation `/dev-reviewer` fix-all re-reviewed APPROVE; pending human approval.** Adds `network_rate_counters`, shared fixed-window abuse controls, admin health dashboard, dry-run replay, audited reveal, override route, bounded aggregate metrics, current route integrations, workspace-mode admin API hard-404, global-pause send protections, fail-closed pause UI, true action totals, structured reason enforcement, and broader anti-persona scrub. Watch-feedback remains dependency-bound to Brief 275. |
| **287** — Closeout — review-checklist gates + dictionary + regression + parent-270 acceptance | **IMPLEMENTED & per-brief reviewed APPROVE 2026-05-19; closeout-checkpoint reviewer caught two defects (misattribution of pre-existing review-checklist items #18/#19 + in-session expansion of `Possible Connection`); both fixed under "fix all". Closeout set (285, 286, 287) ready for fresh-context closeout-checkpoint re-review; pending human approval to close parent Brief 270.** Appends 7 durable boolean Network trust gates to `docs/review-checklist.md` (#20-#26 — items #18/#19 pre-existing from 2026-05-18 ref-doc reconciliation, uncommitted), finalizes 3 Brief 270 canonical terms in `docs/dictionary.md` (Invitation Candidate, Discovery Profile, Claim Invite — Brief 271 terms strictly unchanged), and adds `pnpm run test:superconnector` single-command regression chaining 8 focused engine vitest files + 2 web component tests + `tsc --noEmit` (120/120 green). No new engine/product/UI code. |
| **279** — Outbound Discovery and Claim Invites | **IMPLEMENTED & fresh-context reviewed APPROVE 2026-05-19; pending human approval.** Adds public/source-registry discovery with LinkedIn pointer-only posture, internal Discovery Profiles, source-backed scoring/suppression, operator-reviewed claim invites, claim tokens, claim-token identity verification, source-row privacy export/delete/purge, retention coverage, claim/decline/delete flow, and guarded discovery/compose/send tools. Residual follow-up: not a full outbox after provider acceptance. |
| 275-277 — Network Superconnector chain (parent 270) | Draft; build resumes after Brief 279 review/approval, then 275-277, then the 285-287 closeout. |
| 271 — Network Doctrine/IA/Superconnector copy | Implemented; pending human approval. |
| 254 (255–261) — Network two-sided front door | Built through 261; closeout pending re-review/approval. |
| 207 / 208 — Vision analysis / Slack adapter | Draft; builder-claimable; no upstream deps; parallelizable. |
| 182–186 / 187 / 189–196 / 201–206 / 113 | Backlog (browser-write family / managed-cloud OAuth / RSI family / Hire-a-Specialist / Zapier-parked). See roadmap + briefs; PM sequences. |

Completed briefs live in `docs/briefs/complete/`.

## Next Steps (sequencing is the PM's call — `/dev-pm`)

What this cycle establishes:
1. ~~Architect reconciliation pass~~ **DONE 2026-05-18** (Reference-doc drift Known Gap closed; docs realigned to post-280/281 IA; Insight-235/236 absorbed).
2. ~~Brief 278 Architect pass~~ **DONE 2026-05-18** — Architect design COMPLETE + fresh-context reviewed (APPROVE-WITH-MINOR-REVISIONS → all B1+B2+N1–N8 fixes applied). Output: parent Brief 278 rewritten + 6 sub-briefs (282-287) + Insight-238 (Curate is the 7th human job). Pending human approval.
3. ~~Foundation checkpoint (282+283+284)~~ **DONE 2026-05-19** — all three sub-briefs implemented + fresh-context reviewed APPROVE. **Brief 279** implemented + reviewed APPROVE. **Briefs 285 + 286 + 287** all implemented + per-brief reviewed PASS/APPROVE 2026-05-19 — closeout set ready for the human-triggered **closeout-checkpoint review** (parent Brief 278 §Review Process step 3) spanning Briefs 270-279 + sub-briefs 282-287 + architecture.md + review-checklist (with the 7 new gates) + implemented diffs. After that review and human approval, parent Brief 270 closes. Build order completed: 271→272+273→274→282→283→284→**279 → (275→276+277 deferred to follow-up)→285+286→287 (closeout checkpoint → closes parent 270 after human acceptance)**.
4. **Human ratification asks (Insight-238 + AC #27 retention defaults)** can be answered any time after design approval, before closeout brief 287. The Documenter records the rulings in `docs/human-layer.md` (Curate as 7th job) + the Brief 270 dictionary (retention defaults).
5. **Brief 284 non-blocking follow-ups:** both RESOLVED 2026-05-19. Email-challenge outer rate-limiting wired into export/delete challenge routes by Brief 286 (`checkEmailChallengeRateLimit` calls in both privacy routes before `initiateEmailChallenge`). `system_retention` audit class added to `networkAuditEventClassValues` and wired into `network-retention.ts` by the Brief 284 dev-review + fix-all pass (this session). See Insight-239 for the broader validate-before-mint rule that pass also produced.
6. Approval debt to clear (non-blocking): **Brief 271** (implemented, pending human approval), **Parent Brief 254** (255–261 built, closeout re-review/approval pending).
7. Then **`/dev-pm`** to sequence the broader backlog (vision/Slack gap closures — Briefs 207/208 builder-claimable; RSI family; Hire-a-Specialist UX).

## Retrospective — Brief 291 build/review/dev-review + cleanup (2026-05-19)

- **Worked well** — The fresh-context review caught three high-impact holes that unit tests alone did not imply: attribution writes needed to authenticate the signed landing cookie, cross-apex handoff tokens needed host binding at `{handle}.ditto.you`, and visitor CTAs needed to stay clickable with inference as a hint only. The fixes stayed local to the signed-cookie helper, middleware, destination pages, attribution route, and CTA strip; the targeted vitest suite plus type-check gave a tight regression gate.
- **Surprised** — Migration ownership was the hidden dirty-worktree risk. Brief 289, Brief 291, and the background-watch draft had all touched adjacent journal slots, so the code could pass while the migration story was still hard to review. Splitting `0015`/`0016`/`0017` made the provenance legible again and prevented Brief 293 from accidentally inheriting Brief 291's table.
- **Change** — Treat any fresh-context review that changes database ownership as a documentation task, not just a code fix. The closeout should always re-check `drizzle-kit check`, roadmap brief paths, downstream migration indices, and `docs/state.md` chronology before marking a sub-brief complete. Playwright remains intentionally documented as not run until the local `/people` render can be exercised without a live Network DB dependency.

## Retrospective — Brief 290 build/review/dev-review + fixes (2026-05-19)

- **Worked well** — The layered quality stack caught what each layer alone would have missed. The interaction e2e (`network-share-studio`) exposed the fetch-effect cancellation race that *both* static review and the fresh-context maker-checker passed clean — a runtime-only failure (X tab stuck "loading") invisible to reading the diff. Then the report-only `/dev-review` 5-pass, run *after* the maker-checker had already returned APPROVE, surfaced 6 more non-blocking items (a missing WAI-ARIA tabs pattern, a partial close-reset that left derived state, a missing story-PNG cache header, three doc/comment gaps) — all self-contained enough that `fix all` stayed tractable and the 41-test + e2e contract survived the tabpanel-wrapper a11y change untouched. Engine-first discipline held: 291/292's shared rate-limit + audit enums were registered once by 290 in closed `as const` arrays so the later sub-briefs extend rather than re-edit (collision-safe by construction).
- **Surprised** — The cancellation race was a textbook recurrence of **Insight-204** (loading/status flag in effect deps re-fires and stomps in-flight state), mirror-imaged: here the effect's cleanup `cancelled` flag killed the very fetch a status mutation had re-triggered. This is the **2nd independent occurrence**, and both times it was invisible to static reading *and* to a genuinely separate fresh-context reviewer — only a delayed/interaction e2e assertion exposed it. An insight we'd written and thought understood recurred anyway, which is itself the signal that the human-process mitigation (review lenses) is structurally insufficient for this bug class.
- **Change** — Build the lint rule Insight-204 nominates as a "future consideration": flag `loading`/`isPending`/`status`/`fetching` identifiers in a `useEffect` deps array when the effect body also calls a `setX` setter (or ties a cleanup `cancelled` flag to an in-flight async). Two occurrences, both review-invisible, is the recurrence bar. Insight-204's own architecture.md Layer-6 ("stream completion must not stomp state") and review-checklist Layer-6 landing items remain UN-applied and are flagged for the Architect — the insight stays **active** until absorbed; the lint rule is the durable fix the human process cannot substitute for.

## Retrospective — Brief 284 dev-review + fixes (2026-05-19)

- **Worked well** — The 5-pass progressive-depth `/dev-review` did its job after the fresh-context maker-checker had already returned APPROVE. The two passes saw with different lenses: the maker-checker confirmed every AC and the architecture-checklist gates were satisfied; the 5-pass audit caught a wasted-side-effect ordering bug (action validated *after* the wrapper-run mint), an O(N·D) sweep pattern, and a couple of UI/quality nits. Defense-in-depth across reviewer lenses paid off — finding 3 MEDIUM + 3 LOW issues on a brief that the maker-checker had already cleared was the design working as intended, not a maker-checker failure. The `fix all` pass was tractable because each finding was self-contained: one schema enum addition, one validation reorder per route, one engine refactor, two small UI/doc edits.
- **Surprised** — The validate-before-mint gap was a fresh deviation from a rule we'd thought was settled. Insight-180 (step-run guard), Insight-211 (no self-HTTP), Insight-232 (wrapper-run primitive) together looked complete; the missing rule was about what flows *into* the wrapper-run insert, not whether the wrapper exists. The privacy routes had already validated `subjectType` against an allowed set up front — `action` should have followed the same pattern, but didn't, and that asymmetry rode through every reviewer until the 5-pass security lens. The honest fix is small (move two lines above one call); the durable lesson is the rule shape, captured as Insight-239 and pointed at architecture.md + review-checklist.md for absorption.
- **Change** — When the Builder writes an audited HTTP route that uses an `action` discriminator (or any other field that flows into the wrapper-run `route` / `sessionId`), the AC checklist and the review checklist should explicitly call out "validate the discriminator before `createNetworkLaneStepRun`" alongside the existing "reject caller-supplied `stepRunId`" criterion. This is the same shape as Brief 284's existing `subjectType` rejection — generalising it to all mint-persisted fields is the work. Insight-239 nominates the rule; Brief 287 (closeout review-checklist gates) is the natural home if it ships before the architecture absorption pass.

## Retrospective — Brief 283 build/review loop (2026-05-18)

- **Worked well** — Treating compliance as a pure classifier plus guarded send/route seams kept the blast radius small: policy blocks, suppression writes, email headers, and complaint ingestion are each independently testable, with the actual outbound send still owned by later briefs.
- **Surprised** — The first review found a subtle state-vs-event bug: suppression idempotency is correct for contact blocking, but complaint circuit breakers must count distinct complaint events after Svix dedup, not durable suppression rows. The fix uses Brief-282 audit events as the event stream and keeps suppressions as state.
- **Change** — Future safety counters should name whether they count events or current state in the brief and tests. If retries are deduped by an external delivery id, that id is the only reason to skip a counter increment. External-delivery dedup claims must either be in the same transaction as the protected side effects or carry an explicit pending/complete state so a failed handler does not permanently mask a retry.

## Retrospective — Brief 282 build/review loop (2026-05-18)

- **Worked well** — The substrate split paid off: schema/migration, audit writer, pure scrubber, share surfaces, and doc state could be tested independently before 283/284 start writing through them. The JSONL wrapper-run check gives the audit/share side effects a concrete server-minted proof instead of accepting caller-shaped strings.
- **Surprised** — The first two reviews both found real bypasses outside the obvious new table path: direct `NetworkCardSilhouette` rendering and `generateShareVariants` accepting fake step ids. Privacy and guard rules need to be enforced at every reusable entry point, not only at the public route seam.
- **Change** — Future Network side-effect helpers should share the stricter `requireServerMintedNetworkLaneStepRunId` path or explicitly wrap harness step ids with `createNetworkLaneStepRun`; tests should include well-formed forged `network-lane-step:*:<uuid>` values, not only missing/falsy/web-direct ids.

## Retrospective — Brief 273 closeout (2026-05-18)

- **Worked well** — The review forced the safety seams to become executable contracts: identity persistence, actor ownership, request update semantics, wrapper-step-run rejection, prompt boundaries, and landing handoff are now all covered by focused tests instead of relying on UI intent.
- **Surprised** — The full-suite failures were not Brief-273 logic, but they still mattered: a "known unrelated baseline" can mask real regressions and weakens reviewer trust. Closing the provisioning mock and network-tier test isolation made the baseline production-ready again.
- **Change** — Network-tier tests should use the existing `withNetworkDbTransaction` seam whenever production code imports `networkDb`; local/CI unit suites should not require live Supabase env unless the test is explicitly a remote parity smoke. No new insight file: this is already covered by the Brief-263 test helper/ADR-048 posture, and this closeout re-aligned the tests to that existing architecture.

## Retrospective — Brief 281 cycle (2026-05-18)

- **Worked well** — Layered quality gates each caught what the prior layer missed: automated checks → fresh-context maker-checker (APPROVE-WITH-NITS, 16/16 ACs, independently re-verified the read-only/Insight-211/183/235 guarantees rather than trusting Builder claims) → a `/dev-review` 5-pass audit that still surfaced 7 substantive findings the maker-checker hadn't (a memory N+1, inconsistent per-kind `projectSlug` handling, a missing focus trap, ARIA `listitem`-not-name-from-content). Defence in depth paid off precisely because the passes had different lenses.
- **Surprised** — The biggest finding was a *correctness asymmetry*, not a bug on the happy path: `projectSlug` was resolved and applied differently per kind, so a project-scoped recall could leak a memory from another project while correctly scoping processes. It only showed up under cross-kind project filtering — the kind of case unit tests rarely cover until a multi-pass audit forces the question "is this filter applied *uniformly*?" Captured as **Insight-236**.
- **Change** — A shared helper that fans out across heterogeneous kinds must resolve any cross-cutting filter *once* and apply it through a single typed object every collector consumes, with an explicit "unresolved → empty" contract and explicit handling for kinds the filter cannot scope. Per-collector re-derivation of the same filter is the defect class. Insight-236 nominates this; it should be weighed into the Architect reconciliation pass alongside Insight-235.

## Retrospective — Brief 278 Architect pass (2026-05-18)

- **Worked well** — Driving the design from **two parallel reviewed inputs** (Designer `-ux.md` + Researcher `-278-trust-privacy-admin.md` with the Q5 AgentMail-capability resolution) meant 10/11 OQs had a documented technical or UX precedent ready when the Architect sat down. Insight-004 sizing forced the split before the parent grew past one builder's review cycle: 6 sub-briefs each independently testable, with a clean foundation/closeout checkpoint shape. The mandatory fresh-context reviewer caught **two specific defects** the Architect would not have caught alone — the fabricated "documented-system-webhook exception" (no such Insight precedent existed) and the routing scope error (extending `/inbound` would have overloaded a route reserved for `MessageReceived` per Brief 098b).
- **Surprised** — A taxonomy gap (Curate as 7th human job) surfaced as **the same recommendation** from three independent surfaces (`memories-legibility-ux.md` OQ-1, Brief 258 KB-shelf visibility row, Brief 278 Privacy Center). The third recurrence is what makes it real — folding it into Orient+Decide each time has been the repeated deferral, and the deferral is itself the defect. Captured as **Insight-238**, flagged for human ratification (Architect rules, human ratifies, Documenter records — per Insight-043 distributed-doc-ownership). Also surprising: the reviewer's N4 caught a phrasing the Architect had not actually written (no "extends the precedent" string existed in the briefs); resolving N4 still required the right fix (changing 282's "to adopt/extend" to "to follow as precedent" + explicit "282 does not modify `connection-proposal.ts`"), but it was a reminder that a reviewer's named string is a *signal*, not a literal grep target.
- **Change** — When the Architect produces a multi-document package (parent + sub-briefs + insight) under Insight-004 sizing, the **fresh-context reviewer should be briefed to score blocking findings by *type-of-defect*, not surface-of-defect** — B1 (fabricated precedent) and B2 (routing scope) sat in different documents but were both *integration-seam* errors; the reviewer flagged them as such and that framing made the fixes coherent across all 8 docs in one pass. The Documenter should preserve this pattern as a heuristic when this brief closes.
